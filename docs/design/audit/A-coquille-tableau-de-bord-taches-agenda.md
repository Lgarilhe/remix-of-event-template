# Audit A : Coquille, tableau de bord, tâches, agenda

> Revue du 25 septembre 2026, en lecture seule, sur `main` au commit `5882084`, avant les lots 1 et 2 du chantier design. Les captures citées viennent du banc visuel local (`docs/design/05-banc-visuel.md`) et ne sont pas versionnées. Les textes entre guillemets sont cités tels qu'ils apparaissent dans l'application. Statut de chaque constat : `docs/design/02-constats.md`.

Audit design et UX en lecture seule, 2026-09-25, sur le code commité de `main` (HEAD `58820844`).
Étalon : direction « calm premium » (neutres chauds teinte 40, bouton principal monochrome, un accent indigo #9a90dd rationné aux signaux, aucun effet décoratif, Instrument Sans 500/600/700, 6 paliers de texte, 4 hauteurs de contrôle, 3 rayons, 150-250 ms, états explicites, vouvoiement).

Statut : TERMINÉ (57 constats).

## Méthode
- Inventaire de référence du 9 septembre : branche `origin/claude/app-details-exhaustifs-s6nf9t`, fichiers `docs/design-system/02-shell-dashboard.md`, `09-calendrier-taches-ia.md`, `README.md`.
- Vérification ligne à ligne sur le code commité (63 fichiers du périmètre, relevés `grep` reproductibles) ; captures réelles (données de démo) dans `scratchpad/captures/avant/`, plus des captures et mesures de contrôle dans `scratchpad/audit/A-captures/` (script `capture-A.mjs`, mesures `metrics.json`) et des calculs de contraste dans `scratchpad/audit/contrast_A.py`.
- Chaque citation `fichier:ligne` a été revérifiée contre `HEAD` (`scratchpad/audit/A-citations-check.txt`).
- Statut vs 9 septembre : « toujours vrai » = relevé dans l'inventaire et inchangé ; « corrigé » = ne se vérifie plus sur `HEAD` ; « nouveau » = absent de l'inventaire (défaut apparu depuis, notamment avec les lots 5-6, ou non relevé alors).

## Tableau des constats

| ID | Prio | Écran | Constat | Preuve | Correctif proposé | Statut vs 9 sept. |
|----|------|-------|---------|--------|-------------------|-------------------|
| A-01 | P2 | Coquille | L'en-tête de 48 px ne contient plus que le bouton de la barre (la cloche est partie avec les lots 5-6, et le `children` n'est jamais passé) : une bande vide sur toutes les pages, sans titre, sans fil d'Ariane, sans actions. | `AppHeader.tsx:53-62`, `AppLayout.tsx:44` (`<AppHeader />` sans enfants) ; capture `dark-desktop/dashboard.png` (bande vide en haut) | Soit supprimer la bande sur ordinateur (bouton de repli dans l'en-tête de la barre, Ctrl B), soit en faire le vrai en-tête de page : titre de la page, fil d'Ariane mission, emplacement d'actions. | toujours vrai (aggravé : plus que le bouton) |
| A-02 | P2 | Coquille | Le nom accessible du bouton de la barre est en anglais, « Toggle Sidebar » : `SidebarTrigger` rend son propre `<span sr-only>` et ignore l'icône passée en enfant. Sur téléphone, c'est l'unique porte vers la navigation. | `ui/sidebar.tsx:242` ; enfants ignorés : `AppHeader.tsx:29-31`, `:57-59` | Passer `aria-label="Afficher la navigation"` / « Replier la barre latérale », et faire rendre `children` par `SidebarTrigger`. | nouveau |
| A-03 | P2 | Coquille (mobile) | Sur téléphone, le seul accès à la navigation est un bouton de 32 × 32 px (`h-8 w-8`), en dessous des 44 px que les lignes de la barre respectent (`min-h-11`). | `AppHeader.tsx:7-8` (`TRIGGER_CLASS`), `:29` | `min-h-11 min-w-11` sur téléphone, comme `SidebarRow`. À remettre à la session barre latérale (D37). | nouveau |
| A-04 | P1 | Coquille | Les bandeaux d'essai et de crédits IA sont rendus par `OrganizationGuard`, donc AU-DESSUS de `AppLayout` (routes `<OrganizationGuard><AppLayout>`), alors que la barre latérale est `fixed inset-y-0 z-10` : sur ordinateur, les 256 premiers pixels du bandeau passent sous la barre. Le texte de `TrialBanner` est aligné à gauche : « Essai : N jours restants » est masqué, seul « Choisir un plan » reste visible. Les commentaires décrivent un placement qui n'existe plus (« rendus dans <main> », « sous l'en-tête »). | `App.tsx:168-191` (ordre des gardes), `OrganizationGuard.tsx:52-66`, `TrialBanner.tsx:2`, `:33-37`, `ui/sidebar.tsx:200` ; capture de contrôle `audit/A-captures/banners-dark-desktop.png` (essai simulé à J-3, crédits à 4 %) : seul « Choisir un plan » est visible ; `metrics.json` : bandeau d'essai à x=0 sur 1440 px de large, barre fixe à x=0 sur 255 px de large et 900 px de haut | Rendre les bandeaux dans `<main>` (via un emplacement de `AppLayout`, sous l'en-tête), avec une seule forme de bandeau (`Banner` : ton, texte, action, fermeture). | nouveau |
| A-05 | P2 | Coquille | Deux bascules de thème : celle de la palette n'écrit pas `konekt-theme` (le choix est perdu au rechargement) et ne met pas à jour l'état `isDark` de la barre, qui garde alors le mauvais libellé (« Mode clair » en thème clair) et la mauvaise variante du logo Konekt (logo blanc sur barre claire). L'icône Soleil/Lune de la palette dépend de `dark:` alors que la classe `.dark` n'est jamais posée : c'est toujours le soleil. | `NavigationPalette.tsx:79-88`, `:182-183` ; `AppSidebar.tsx:73-86`, `:224-229` ; `main.tsx:105-113` (seule `.light` est posée) | Un seul `useTheme()` (état, stockage, classe) partagé par la barre, le menu et la palette ; icône choisie en JS, pas par `dark:`. | toujours vrai (persistance) + nouveau (désynchronisation, icône) |
| A-06 | P2 | Coquille | Le vocabulaire de navigation n'est pas unifié : « Dashboard » (palette, titre SEO) contre « Tableau de bord » (barre, fenêtre des raccourcis) ; « Agenda » (barre, palette, raccourcis) contre « Calendrier » (titre et SEO de la page, lien du tableau de bord) ; « Inbox » (tableau de bord) contre « Messagerie » (palette, raccourcis) ; icônes différentes pour la même page (Missions : `Target` / `Briefcase` ; Tâches : `CheckSquare` / `ListTodo` ; Pipeline : `Kanban` / `Columns3`). | `NavigationPalette.tsx:106-143` ; `AppSidebar.tsx:99`, `:136` ; `KeyboardShortcutsDialog.tsx:25-31` ; `SidebarBottomRow.tsx:47-50` ; `SidebarTabs.tsx:40-44` ; `MissionsPanel.tsx:106` ; `Dashboard.tsx:175` ; `Calendar.tsx:262`, `:279` ; `DashboardTodayPanel.tsx:248` ; `DashboardGreeting.tsx:205` | Un registre unique des pages (libellé, icône, route, raccourci) lu par la barre, la palette, la fenêtre des raccourcis, les titres de page et les liens du tableau de bord. | partiellement corrigé (G D/G M/G P désormais implémentés par `GoShortcuts.tsx` ; la page `/agents` s'intitule bien « Assistant ») + nouveau (Agenda/Calendrier) |
| A-07 | P3 | Coquille | La palette « Aller à » n'a pas de titre de dialogue : fenêtre sans nom pour un lecteur d'écran (et avertissement Radix). | `ui/command.tsx:26-36` (`CommandDialog` sans `DialogTitle`) | `DialogTitle` masqué « Aller à ». | nouveau |
| A-08 | P2 | Coquille (erreurs) | Les trois écrans d'erreur globaux gardent le registre « brutal » : aucun rayon, capitales espacées, carré noir plein, message technique brut en police mono (tronqué à 150/200 caractères), classes vestigiales `relative overflow-hidden group` / `relative z-10`. | `ErrorBoundary.tsx:64-95`, `SectionErrorBoundary.tsx:40-55`, `OrganizationGuard.tsx:25-44` | Composant `ErrorState` partagé : titre humain, phrase d'explication, « Réessayer » en bouton principal monochrome, lien secondaire, détails techniques repliés. | toujours vrai |
| A-09 | P3 | Coquille (erreurs) | La barrière d'erreur de l'assistant est montée à la racine, après les routes : si l'assistant plante, le bloc d'erreur (`my-4`) s'ajoute sous la page, hors de la vue. | `App.tsx:195-199`, `SectionErrorBoundary.tsx:40` | Repli propre au tiroir (petite carte fixe ou toast avec « Rouvrir l'assistant »). | nouveau |
| A-10 | P2 | 404 | Page 404 inchangée : `<a href="/">` recharge toute l'application et renvoie un utilisateur connecté vers la landing publique (sans jeton d'aperçu), lien en capitales soulignées, aucune explication ni marque, « 404 » en capitales espacées. | `NotFound.tsx:13-24` ; capture `dark-desktop/notfound.png` | État vide pleine page : logo, « Cette page n'existe pas », une phrase, bouton principal « Retour au tableau de bord » (`Link`, jeton conservé), lien « Aller à… (Ctrl J) ». | toujours vrai |
| A-11 | P3 | Coquille | Quatre spinners maison différents dans la coquille (tailles 36/28/24 px, un seul avec `role="status"` et libellé), plus « Chargement en cours » en capitales ; aucun ne respecte `prefers-reduced-motion`. | `App.tsx:136-145`, `AppLayout.tsx:22-27` (le bon : `role=status` + sr-only), `OrganizationGuard.tsx:12-16`, `ProtectedRoute.tsx:13` | Un seul `Spinner` (taille, libellé lu, animation coupée en mouvement réduit). | toujours vrai |
| A-12 | P2 | Coquille | Le bouton « Se reconnecter » de la fenêtre de session expirée est peint en bleu `bg-info` : troisième couleur d'action, contraste blanc sur bleu 3,6:1 (sous 4,5:1). | `SessionExpiredDialog.tsx:50-55` | `Button variant="primary"` (monochrome). | toujours vrai |
| A-13 | P3 | Coquille | `PageLayout` pose `min-h-screen` sous un en-tête de 48 px : toute page qui l'utilise mesure au moins 100 vh + 48 px, donc défile toujours d'au moins 48 px, même courte. | `PageLayout.tsx:39`, `AppHeader.tsx:53` (`h-12`), `AppLayout.tsx:41-45` ; mesure `audit/A-captures/metrics.json` : sur `/calendar` (1440×900), la page mesure 948 px = 48 + 900, alors que le dernier contenu visible (ligne des raccourcis) s'arrête vers 720 px (`calendar-week-full-dark-desktop.png`) | `flex-1` / `min-h-full` au lieu de `min-h-screen`. | nouveau |
| A-14 | P3 | Coquille | Primitives de mise en page mortes ou divergentes : `PageHeader`, `Section`, `layout/EmptyState`, `StatTile`/`StatGrid` ne sont importés nulle part ; seul `PageLayout` sert (Dashboard, Tâches, Agenda). Deux `EmptyState` concurrents, dont `ui/EmptyState` qui colle une icône `Settings` sur tout bouton d'action. | `grep` des imports : `Calendar.tsx:57`, `Dashboard.tsx:24`, `Tasks.tsx:37` seuls ; `ui/EmptyState.tsx:53-59` | Réécrire `PageHeader`, `EmptyState`, `ErrorState` dans le registre cible, puis les imposer ; supprimer l'autre `EmptyState`. | toujours vrai |
| A-15 | P3 | Aide | L'aide n'est pas contextuelle : « Aide > Vidéo du tutoriel » ouvre toujours le tutoriel du pipeline, quelle que soit la page (un seul tutoriel existe). La puce des points est en `text-brand-purple` (violet de l'ancienne marque). | `AppSidebar.tsx:240-245`, `help/tutorials.ts:12-22`, `TutorialVideoDialog.tsx:123`, `:100` (`font-display text-[16px]`) | Table route → tutoriel ; entrée masquée quand la page n'en a pas ; puce neutre. | partiellement corrigé (le tutoriel est désormais monté via le menu Aide) + nouveau |
| A-16 | P2 | Transverse | Anneau de focus insuffisant : `--ring` vaut blanc à 25 % en sombre (2,28:1 sur le fond) et `40 3% 60%` en clair (2,69:1) ; la barre utilise `--sidebar-ring`, identique (2,24:1). Seuil WCAG 2.1 : 3:1. | `index.css:40`, `:52`, `:158`, `:167` ; calcul `audit/contrast_A.py` | Anneau = accent indigo (`#9a90dd` en sombre, 5,99:1 sur le fond), 2 px, décalé de 2 px. | toujours vrai |
| A-17 | P2 | Transverse | Typographie : la règle globale force `h1, h2, h3` en Outfit 700, donc tous les titres de la coquille (titre du panneau de la barre, titres de section) sortent en Outfit ; `font-display` est posé à la main (nom de l'organisation). Six familles chargées (Space Grotesk, Space Mono, Outfit, Instrument Serif, Instrument Sans, Bricolage). | `index.css:241-244`, `index.html:22-25`, `AppSidebar.tsx:112`, `:117`, `:186`, `SidebarSection.tsx:143-145` | Retirer la règle `h1,h2,h3`, faire de `font-display` un alias d'Instrument Sans le temps de la migration, ne charger que Instrument Sans (500/600/700). | toujours vrai |
| A-18 | P3 | Barre latérale (à remettre à la session barre latérale) | Tailles arbitraires, dont deux sous le plancher de 10 px : `text-[9px]` (compteur replié des tâches en retard), `text-[9.5px]` (initiales des réponses), puis 10, 10.5, 11, 11.5, 12, 12.5 et 13 px. | `SidebarBottomRow.tsx:84`, `RepliesSection.tsx:58`, `MissionNavRow.tsx:121`, `:126-127`, `:165`, `:173`, `SidebarRow.tsx:74`, `:82`, `:92`, `SidebarSection.tsx:61`, `:145` | Six paliers nommés (10/11/12/13/14 px + titres), plancher 10 px. | nouveau (code des lots 5-6) |
| A-19 | P3 | Barre latérale (à remettre à la session barre latérale) | Couleur brute et variante morte : la coche des premiers pas est `text-green-600 dark:text-green-500` ; `dark:` ne s'applique jamais (aucune classe `.dark`). | `FirstStepsSection.tsx:145` | `text-success` (ou neutre, puisque l'étape est grisée). | nouveau |
| A-20 | P3 | Barre latérale (à remettre à la session barre latérale) | Cibles tactiles incomplètes : « Aller à… » `h-9` (36 px) sans `min-h-11` sur téléphone ; déclencheur du menu de l'avatar ~40 px ; ses entrées sans `min-h-11`, contrairement au menu Aide. | `AppSidebar.tsx:146`, `SidebarUserMenu.tsx:71-74`, `:125-166` vs `HelpMenu.tsx:70`, `:77` | Aligner sur la règle des lots 5-6 (`min-h-11 md:min-h-8`). | nouveau |
| A-21 | P3 | Barre latérale (à remettre à la session barre latérale) | Pastilles de compteur en `bg-primary` (blanc sur sombre) alors que la cible réserve l'accent indigo aux compteurs ; marque « KONEKT » en 10 px capitales à 40 % d'opacité (3,05:1 en sombre, 1,95:1 en clair) ; solde de crédits « ... » (trois points ASCII) et « n/d ». | `SidebarTabs.tsx:105`, `AppHeader.tsx:36`, `AppSidebar.tsx:231`, `SidebarUserMenu.tsx:55-58` | Décider accent ou neutre pour les compteurs et l'appliquer partout ; marque en `text-muted-foreground` sans capitales ; « … » et « Indisponible ». | nouveau |
| A-22 | P2 | Tableau de bord | Registre décoratif, à l'opposé de la cible : dégradé et tache floue derrière l'accueil, emoji animé dans le titre (🌙 ☀️ 👋), point « session active » qui pulse en boucle, halos pulsants sur les cartes actives et les états « à jour », pings des canaux, `LivePulse` : 7 animations `repeat: Infinity`. L'icône IA `Sparkles` sert aussi à « Inbox », au récapitulatif de la semaine et à l'état vide des missions. | `DashboardGreeting.tsx:76`, `:82-88`, `:39-44`, `:141-149`, `:120-129`, `:204` ; `DashboardFocusPanel.tsx:112-119`, `:226-231` ; `DashboardTodayPanel.tsx:656-661` ; `DashboardWeekHighlight.tsx:288-295`, `:304` ; `DashboardConnections.tsx:104-111` ; `LivePulse.tsx:34-39` ; `DashboardMissionsPanel.tsx:378` | Retirer dégradé, tache, emoji, halos et pings ; un point fixe suffit pour « en direct » ; `Sparkles` réservé à l'IA. | toujours vrai |
| A-23 | P1 | Tableau de bord | Le mouvement réduit n'est pas respecté : 48 balises `motion.*` dans le tableau de bord, aucune ne consulte `prefers-reduced-motion` (seul `AppLayout` le fait) ; le commentaire de `LivePulse` affirme le contraire. Mesuré avec `reducedMotion: 'reduce'` : halo encore animé (opacité 0,36 puis 0,30 à 700 ms d'écart) ; au chargement, « Mes missions » glisse par-dessus « Pour aujourd'hui » (animation de disposition de `Reorder`). Durées et délais cumulés jusqu'à ~1,2 s (tache 1,2 s, statistiques 0,4 à 0,7 s + 0,4 s), loin des 150-250 ms visés. Les boucles infinies démarrent seules, durent plus de 5 s et n'ont pas de pause : critère WCAG 2.2.2 (niveau A). | `LivePulse.tsx:8` vs `:34-39` ; `AppLayout.tsx:31`, `:53` ; `DashboardGreeting.tsx:87` ; `DashboardWeekHighlight.tsx:120`, `:338-366` ; `audit/A-captures/metrics.json` (`haloOpacityT0`/`T700ms`), `dashboard-early-reduced-motion.png` ; capture `avant/dark-mobile/dashboard.png` (contenu encore à demi transparent) | `MotionConfig reducedMotion="user"` à la racine ne suffit pas (il laisse passer les boucles d'opacité) : supprimer les boucles, une seule entrée de 200 ms par page. `useCountUp` respecte déjà la préférence. | toujours vrai (désormais mesuré) |
| A-24 | P2 | Tableau de bord | Courbes cassées : `Sparkline` lit `hsl(var(--success))` et `hsl(var(--info))`, variables qui n'existent pas (les jetons s'appellent `--status-success`, `--status-info`). Résultat mesuré : trait `none` et aire remplie en noir pour « Contactés », « Réponses », « Placements » ; en thème clair, trois triangles noirs. | `Sparkline.tsx:30`, `:33` ; `index.css:67-75` ; `audit/A-captures/metrics.json` (`sparks`) ; `crop-dashboard-week-light.png`, `crop-dashboard-bottom.png` | Classes `stroke-success` / `fill-success/15` (jetons Tailwind), ou `hsl(var(--status-success))`. | nouveau |
| A-25 | P2 | Tableau de bord | États de chargement et d'erreur absents : quatre sections rendent `null` pendant le chargement (la page se reconstruit par sauts) ; aucune section n'a d'état d'erreur, les lectures sous-jacentes avalent les erreurs (rappels, agenda), donc une panne s'affiche « Rien à l'agenda » ou « Aucun rappel ». | `Dashboard.tsx:125-169` ; `useAllReminders.ts:76-85` ; `useCalendarEvents.ts:166-191` (champ `error` ignoré) ; `DashboardTodayPanel.tsx:269-276` | Squelette à hauteur fixe par section ; `ErrorState` compact par section avec « Réessayer », sur le modèle de `SidebarSection`. | toujours vrai |
| A-26 | P2 | Tableau de bord | Cinq teintes dans la seule rangée « Pour aujourd'hui » (info, warning, destructive, success + tuile émeraude) ; les rappels à traiter sont peints en vert « succès » ; pastilles de statut de mission en capitales colorées ; palettes de 8 teintes brutes pour les initiales, avec des `dark:` morts : en sombre, initiales `-700` sur tuile `/15` à 1,7-2,3:1. 18 `emerald-*` et 60 couleurs de palette brute dans la zone. | `DashboardFocusPanel.tsx:51-83`, `:125`, `:211` ; `DashboardMissionsPanel.tsx:47-52`, `:132-139`, `:310` ; `dashboard/CandidateAvatar.tsx:31-40` ; `MissionCompanyLogo.tsx:47-56` ; `audit/contrast_A.py` | Cartes neutres, accent indigo sur le seul chiffre qui compte, statut = point + texte neutre ; initiales neutres comme `MissionNavRow` (`bg-muted text-muted-foreground`). | toujours vrai + nouveau (contraste mesuré) |
| A-27 | P2 | Tableau de bord, Tâches, Agenda | Logos devinés chez des tiers : `MissionCompanyLogo` appelle `logo.clearbit.com` puis `google.com/s2/favicons` depuis le navigateur avec le nom du client (fuite vers des tiers, tuile `bg-white` en dur). Dans Tâches, la « société » passée est l'intitulé du poste : domaine absurde (« leaddeveloperbackend.com ») et pastille rouge « LB » collée à l'avatar, qui se lit comme une alerte. La barre latérale a fait le choix inverse (aucune image réseau). | `MissionCompanyLogo.tsx:71-80`, `:113` ; `Tasks.tsx:641-644` ; `MissionNavRow.tsx:4-5` ; capture `avant/dark-desktop/tasks.png` (pastilles « LB ») | Logo seulement s'il est stocké (`logo_url`), sinon initiales neutres ; jamais déduit d'un intitulé de poste. | toujours vrai |
| A-28 | P2 | Tableau de bord (mobile) | Réorganisation : sur téléphone, la poignée (28 px, toujours visible, `absolute right-2 top-2`) chevauche ce qui est déjà à droite de chaque section (« Action requise », le compteur, « Voir tout ») ; sur ordinateur elle est invisible hors survol, y compris au focus clavier, et sort du conteneur (`-left-7`) ; aucun moyen de réordonner au clavier. | `DashboardSortableItem.tsx:45-63` ; `crop-mobile-dashboard-handles.png`, `metrics.json` (`handles` à 20 px du bord droit) | Mode « Personnaliser » explicite (liste avec Monter/Descendre), ou abandon du réordonnancement. | toujours vrai + nouveau (chevauchement) |
| A-29 | P3 | Tableau de bord | Carte mission : le chevron de détail est invisible hors survol (y compris au clavier et au doigt), cible de 24 px ; le résumé « 140 · 24 · 12 » n'est porté que par trois icônes (`Users`, `Send`, `CheckCircle2`), sans libellé visible ni nom accessible. Le chevauchement signalé le 9 septembre n'est pas reproduit (l'enveloppe est `relative`). | `DashboardMissionsPanel.tsx:174-185`, `:150-161`, `:394` | Bouton « Détails » visible ; « 140 sourcés · 24 contactés · 12 retenus ». | toujours vrai (survol) + corrigé/non reproduit (chevauchement) |
| A-30 | P3 | Tableau de bord | Panneau « Aujourd'hui » : case à cocher des rappels = `<button>` de 20 px avec seulement `title`, sans rôle ni état, bordure à 1,9:1 ; échec silencieux ; navigation par `window.location.href` (rechargement complet) ; sous-titre « 5 événements à venir » qui compte les rappels en retard ; « Live », « 12min », « Séq. » ; « Rejoindre maintenant » blanc sur vert à 2,3:1 ; bouton « + » nommé par `title` seul. | `DashboardTodayPanel.tsx:522-537`, `:198-207`, `:545-547`, `:227-231`, `:391-397`, `:632`, `:481-482`, `:236-242` ; capture `avant/dark-desktop/dashboard.png` (10:07 en rouge, « à venir ») | `Checkbox` du kit, toast et retour arrière en cas d'échec, `Link`, « 2 en retard · 3 à venir », libellés français, bouton principal monochrome. | toujours vrai + nouveau (sous-titre) |
| A-31 | P3 | Tableau de bord | Canaux : statut en 10,5 px capitales grasses colorées (« CONNEXION… » en jaune à 1,9:1 en clair) ; la carte « Non connecté » n'offre aucune action visible (seulement une flèche au survol), alors que le commentaire promet un bouton « Reconnecter » ; « LinkedIn » lu deux fois (alt + nom) ; l'état « connexion » cumule trois signaux (ping bleu, point jaune, texte jaune). | `DashboardConnections.tsx:113-115`, `:122-130`, `:8`, `:182`, `:92`, `:95-111` | Bouton texte « Connecter » / « Reconnecter » ; statut en texte neutre avec un point ; `alt=""`. | toujours vrai |
| A-32 | P3 | Tableau de bord | Texte : titre SEO « Dashboard » ; « Rechercher » mène à `/missions` (pas à la recherche) ; « Inbox » ; « Analytics complet » ; trois libellés pour le même lien (« Voir tout », « Tout voir », « Calendrier ») ; le total « Pour aujourd'hui » (réponses + relances + stagnants + rappels) concurrence le chiffre « À traiter » de la barre, défini autrement. | `Dashboard.tsx:175` ; `DashboardGreeting.tsx:196-208` ; `DashboardWeekHighlight.tsx:324` ; `DashboardMissionsPanel.tsx:349` ; `DashboardActivityFeed.tsx:215` ; `DashboardTodayPanel.tsx:248` ; `DashboardFocusPanel.tsx:215`, `:264-266` ; `lib/sidebarSignals.ts` | Un seul total « à traiter » (celui de la barre) ; libellés « Tableau de bord », « Messagerie », « Voir tout » partout. | toujours vrai + nouveau (double total) |
| A-33 | P3 | Tableau de bord | Code mort : `JobDetailSheet` monté avec un `selectedJobId` jamais renseigné ; import `Plug` inutilisé. | `Dashboard.tsx:88`, `:229-233` ; `DashboardConnections.tsx:19` | Retirer. | toujours vrai |
| A-34 | P1 | Tâches | Une panne se lit comme un succès : `fetchAllReminders` avale l'erreur et renvoie une liste vide ; sur panne simulée, la page affiche « Zéro tâche en cours », une coche verte et cinq compteurs à 0, pendant que la barre latérale, sur la même panne, affiche « Impossible de charger vos entretiens. Réessayer ». Un recruteur peut croire n'avoir aucune tâche en retard. Même source pour « Rappels du jour » du tableau de bord. | `useAllReminders.ts:76-85` ; `Tasks.tsx:404-451` ; capture `audit/A-captures/tasks-error-simulated.png` | Laisser l'erreur remonter (`throw`), `ErrorState` « Impossible de charger vos tâches » + « Réessayer » ; compteurs « – » tant que rien n'est lu. | toujours vrai (désormais démontré) |
| A-35 | P2 | Tâches | Tutoiement dans une page par ailleurs vouvoyée : « tes filtres », « Relâche un filtre », « Crée-en une », « Détectées d'après ton activité — clique pour créer ou rejeter », « Crée un rappel… ou standalone », toast « Saisis un titre », suggestions « Note tes observations + envoie le retour client » ; alors que le sélecteur de périmètre vouvoie (« les tâches que vous avez créées »). Autres défauts : « le modal d'un candidat », « standalone », « prep », « Zéro tâche », et une suggestion qui écrit « Entretien Entretien final ». | `Tasks.tsx:344`, `:427-433` ; `CreateTaskModal.tsx:165`, `:227-229` ; `useAutoTaskSuggestions.ts:156`, `:159`, `:205` ; `TasksFiltersBar.tsx:68` ; capture `avant/dark-desktop/tasks.png` (« Entretien Entretien final ») | Passe de relecture au vouvoiement ; « Aucune tâche en cours » ; « Entretien final terminé il y a 15 h ». | toujours vrai + nouveau (détails) |
| A-36 | P2 | Tâches | Registre : bandeau de suggestions entièrement vert émeraude (fond, filet, motif en capitales vertes, bouton `bg-emerald-600 text-white`) ; cinq cartes de compteurs en cinq teintes ; intitulé de poste en pastille capitales 10 px ; catégories en emoji (📌 🔁 🎯 📝 📂 🤝 🔍) ; « Effacer (n) » peint en rouge destructif pour une action qui ne détruit rien. 17 `emerald-*` dans la zone. | `Tasks.tsx:330-377`, `:365`, `:66-108`, `:663`, `:669` ; `TasksFiltersBar.tsx:57-65`, `:249` ; `CreateTaskModal.tsx:80-88` | Bandeau neutre (filet, texte secondaire), bouton « Créer » secondaire ; catégories en texte ou icônes lucide ; « Effacer les filtres » en bouton fantôme. | toujours vrai |
| A-37 | P2 | Tâches (mobile) | Le contenu utile arrive trop tard : suggestions, cinq cartes de compteurs (trois rangées sur 390 px) et filtres passent avant la première tâche ; les compteurs répètent les en-têtes de section (mêmes seaux, mêmes nombres). | `Tasks.tsx:386-390` vs `:518-534` ; captures `avant/dark-mobile/tasks.png`, `audit/A-captures/tasks-full-dark-mobile.png` | Supprimer la bande de compteurs (les en-têtes de section les portent) ; suggestions repliées sur une ligne « 2 suggestions ». | nouveau |
| A-38 | P2 | Tâches | Contrôles : le segmenté « Actives / Toutes » n'a pas `aria-pressed`, alors que « Mes tâches / Équipe » l'a ; bouton à trois états « Source » (Auto seulement → Manuel seulement → neutre) sans état annoncé ; « Ignorer » nommé par `title` seul ; création depuis une suggestion en échec silencieux (`console.warn`) ; « Actualiser » sans état de chargement ; coche sans retour visuel pendant l'enregistrement ; deux cibles vers le même candidat (avatar et nom) ; hauteurs 28 et 32 px dans la même barre. | `Tasks.tsx:287-303`, `:308-315`, `:371-377`, `:215-218`, `:613-626`, `:630-646`, `:691-699` ; `TasksFiltersBar.tsx:143`, `:146`, `:82`, `:214-243` | `SegmentedControl` partagé (`aria-pressed`), filtre « Origine » en liste (Toutes / Automatiques / Manuelles), toasts d'échec, un seul lien candidat. | toujours vrai + nouveau (périmètre) |
| A-39 | P3 | Tâches | Modale « Nouvelle tâche » : aucun `<label>` n'est associé à son champ (date, heure et catégorie sans nom accessible) ; message d'erreur brut dans le toast ; libellés de catégorie différents entre filtre et modale (« Relance » / « Relance candidat », « Admin » / « Administratif »…) ; toast « Pas authentifié ». La valeur sentinelle `__none__` n'est plus un risque : elle ne passe pas en base. | `CreateTaskModal.tsx:236-311`, `:211`, `:161`, `:80-88`, `:181-195` ; `TasksFiltersBar.tsx:57-65` | `Label htmlFor` + `id` ; une seule table de catégories ; messages d'erreur humains. | toujours vrai + corrigé (sentinelle) |
| A-40 | P1 | Agenda | Une panne se lit comme un agenda vide : `fetchCalendarEvents` ignore le champ `error` de ses trois lectures et `isError` n'est jamais lu ; sur panne simulée des trois sources, la page affiche « Pas d'événement cette semaine » sous une grille vide, alors que la barre dit « Impossible de charger vos entretiens ». | `useCalendarEvents.ts:166`, `:306`, `:333` ; `Calendar.tsx:150`, `:646-664` ; capture `audit/A-captures/calendar-error-simulated.png` | Faire échouer la requête, `ErrorState` + « Réessayer » à la place de la grille. | toujours vrai (désormais démontré) |
| A-41 | P2 | Agenda (semaine) | La vue « Semaine » est une fenêtre glissante qui commence aujourd'hui (vendredi → jeudi sur la capture) ; sept colonnes de ~160 px à 1440 px tronquent les cartes (« Lucas Cle… », l'icône Calendly chevauche « 1ER ») ; la ligne « maintenant » est placée en pourcentage de la hauteur de colonne alors que les cartes sont empilées sans axe horaire : elle traverse la carte au hasard ; un jour vide est un bouton pleine hauteur avec un « + » à 20 % d'opacité. | `Calendar.tsx:134`, `:179-182`, `:469-478`, `:536-544`, `:552-563` ; captures `avant/dark-desktop/calendar.png`, `avant/dark-mobile/calendar.png` | Semaine calée sur le lundi (ou libellé « 7 prochains jours ») ; pas de ligne « maintenant » hors grille horaire ; jour vide = texte discret + bouton « Programmer » au survol et au focus. | nouveau (fenêtre glissante, ligne trompeuse) + toujours vrai (« + » à 20 %) |
| A-42 | P2 | Agenda (jour) | Vue jour : la carte complète est enfermée dans une case de 32 à 56 px (`overflow-hidden`, la prop `compact` n'est pas lue par `EventCard`) : un entretien de 45 min n'affiche que « 12:00 · 45min 1ER », sans le nom du candidat ; deux événements simultanés se superposent ; un événement avant 7 h sort de la zone défilable (position négative) ; défilement imbriqué `max-h-[640px]`. | `CalendarDayView.tsx:63-85`, `:118`, `:163-178` ; `Calendar.tsx:721-733` ; capture `audit/A-captures/calendar-day-dark-desktop.png` | Carte compacte dédiée (heure + nom) sous 70 px ; disposition en colonnes des chevauchements ; plage étendue ou bandeau « avant 7 h ». | toujours vrai (compact, superposition) + nouveau (conséquences mesurées, avant 7 h) |
| A-43 | P3 | Agenda (liste) | Vue liste : six lignes « Pas d'événement » en italique à 60 % d'opacité pour une semaine à un seul entretien ; en-têtes de jour déclarés `sticky` mais dans un conteneur `overflow-hidden`, donc jamais collés ; `<div onClick>` autour du bouton de la carte (double déclenchement). | `CalendarListView.tsx:28`, `:47`, `:77-80`, `:83-89` ; capture `audit/A-captures/calendar-list-dark-desktop.png` | N'afficher que les jours qui ont des événements (ou les regrouper « Rien jusqu'à mardi ») ; retirer le `div` cliquable. | toujours vrai (div) + nouveau (sticky, jours vides) |
| A-44 | P2 | Agenda | Couleurs par type en palette brute, avec `dark:` morts : violet (entretien), cyan (séquence), bleu info (InMail) ; tours : ambre (final), bleu (2e), cyan (3e) : le cyan veut dire à la fois « séquence » et « 3e tour ». Deux tables de couleurs dupliquées avec des opacités différentes. Ombre de la ligne « maintenant » en `rgba` codé en dur. 48 couleurs de palette brute et 20 `emerald-*` dans la zone. | `Calendar.tsx:76-119`, `:795-806`, `:541` ; `EventDetailSheet.tsx:56-80` ; `CalendarDayView.tsx:153` | Types en neutre avec icône ; un seul signal coloré (conflit) ; tour en texte (« 2e entretien ») ; une seule table partagée. | toujours vrai + nouveau (collision cyan) |
| A-45 | P2 | Agenda | Filtres et formulaire : `FilterPill` recopié à l'identique depuis Tâches ; « Mes RDV » sans `aria-pressed` et d'une autre forme que « Mes tâches / Équipe » pour la même notion ; « Téléphone » avec l'icône `Briefcase` ; champ « Sauvegarder cette vue » en `<input>` brut (`focus:outline-none` + anneau à 30 %), libellé non associé, tutoiement « Active des filtres pour sauver » ; suppression d'une vue visible au survol seulement, sans confirmation, nommée par `title` seul ; « Effacer (n) » en rouge. | `CalendarFiltersBar.tsx:108-137`, `:199-211`, `:98`, `:386-419`, `:442-449`, `:463` ; copie identique : `TasksFiltersBar.tsx:72-101` | `FilterBar` / `FilterPill` / `ToggleChip` partagés avec Tâches ; `Input` du kit ; vouvoiement ; bouton de suppression visible au focus. | toujours vrai + nouveau (« Mes RDV » vs « Mes tâches ») |
| A-46 | P2 | Agenda | Modale « Programmer un entretien » : tutoiement (« Crée un événement dans ton calendrier », « Cherche dans tes candidats… », toast « Sélectionne ou crée un candidat ») ; libellés d'étape différents de ceux du filtre (« Qualif initiale / 2e tour client / 3e tour / Final round » vs « 1er entretien / … / Final ») ; formats sans `aria-pressed`, trois sur quatre avec `MapPin` ; libellés non associés ; l'autocomplétion candidat n'a ni rôle `combobox` ni navigation aux flèches, Entrée prend d'office le premier résultat et Échap ferme aussi la modale. | `CreateEventModal.tsx:259-261`, `:317`, `:156`, `:76-81`, `:69-73`, `:412-425`, `:268-452` ; `CalendarFiltersBar.tsx:101-106` ; `CandidateAutocomplete.tsx:143-156` | Vouvoiement ; une table d'étapes ; `ToggleGroup` pour le format ; `Combobox` (cmdk) du kit. | toujours vrai + nouveau (combobox) |
| A-47 | P3 | Agenda | Fiche d'événement : pied à quatre ou cinq actions en trois styles (« Tâche prép. », « Tâche débrief », « Préparer l'entretien (scorecard IA) » teinté vert, « Rejoindre la réunion », sinon « OK » qui ne fait que fermer) ; « Ouvrir le lien » et « Copier » nommés par `title` seul. | `EventDetailSheet.tsx:438-499`, `:404-422` | Une action principale (Rejoindre, sinon Préparer), le reste dans un menu « Plus » ; supprimer « OK » ; `aria-label`. | toujours vrai |
| A-48 | P3 | Agenda | Raccourcis globaux J/K/T/N/1/2/3 et flèches ← → actifs dès que le focus n'est pas dans un champ texte, y compris sur un bouton, dans une liste de sélection ou dans la fiche ouverte ; ligne d'aide des raccourcis en 10 px à 60 % (2,76:1) ; `localStorage` sans `try/catch`. | `Calendar.tsx:196-232`, `:704-716`, `:140`, `:145` | Reprendre la garde de `GoShortcuts` (rôles menu/listbox/dialog) ; aide dans la fenêtre « Raccourcis clavier » du menu Aide. | nouveau |
| A-49 | P3 | Agenda (mobile) | Chaque jour vide garde 260 px : à 390 px, une semaine à un seul événement mesure 2 266 px de haut ; les boutons de vue perdent leur libellé sous `md`. | `Calendar.tsx:484`, `:339` ; `metrics.json` (`scroll-mobile.calendar`) ; `calendar-week-full-dark-mobile.png` | Vue Liste par défaut sur téléphone, jours vides compacts. | toujours vrai + nouveau (mesure) |
| A-50 | P2 | Transverse (visible surtout dans l'Agenda) | Les jetons à alpha intégré cassent les modificateurs d'opacité en thème sombre : `--border` vaut `0 0% 100% / 10%`, donc `border-border/40` devient `hsl(0 0% 100% / 10% / 0.4)`, couleur invalide qui retombe sur `currentColor` (blanc), et `bg-border/40` retombe sur transparent. Mesuré : filet blanc vif au pied de chaque carte d'agenda, lignes d'heures invisibles en vue jour. 89 usages `border-border/NN`, `bg-border/NN`, `divide-border/NN`, `ring-border/NN` dans `src/`. | `index.css:38-40`, `:51` ; `Calendar.tsx:898` ; `CalendarDayView.tsx:142` ; `DashboardMissionsPanel.tsx:224` ; captures `calendar-list-dark-desktop.png`, `calendar-day-dark-desktop.png` ; mesure `check-border.mjs` (sombre : `rgb(250, 250, 250)` pour `border-border/40`, `rgba(0, 0, 0, 0)` pour `bg-border/40`) | Jetons en triplets purs, l'opacité portée par la classe ; ou jetons dédiés `--border-subtle` / `--border-strong`. | nouveau |
| A-51 | P2 | Transverse | Variantes `dark:` mortes : `darkMode: ["class"]` alors que seule `.light` est posée ; les 30 `dark:` du périmètre ne s'appliquent jamais. Conséquences : initiales `-700` sur fond sombre (1,7-2,3:1), icônes `violet-600` / `cyan-600` de l'agenda, texte `emerald-700` du bandeau de suggestions, coche `green-600` des premiers pas, icône Soleil/Lune figée de la palette. | `tailwind.config.ts:4` (état commité), `main.tsx:105-113` ; `dashboard/CandidateAvatar.tsx:31-40` ; `Calendar.tsx:93`, `:109` ; `Tasks.tsx:336`, `:358` ; `FirstStepsSection.tsx:145` ; `NavigationPalette.tsx:182-183` | Faire valoir `dark:` hors `.light`, puis supprimer les couleurs brutes qui en dépendent. | nouveau |
| A-52 | P2 | Transverse | Contraste des statuts en texte : en thème clair, `text-warning` fait 1,9:1 et `text-success` 2,3:1 sur fond blanc (« CONNEXION… », « ACTIVE », échéance « Aujourd'hui · 14:30 », « Tout actif », « 12min ») ; en sombre, `text-destructive` fait 3,54:1, sous 4,5:1 pour les textes de 10 à 12 px (« ACTION REQUISE », dates en retard). | `DashboardConnections.tsx:39-43`, `:157`, `:162` ; `DashboardMissionsPanel.tsx:48` ; `Tasks.tsx:681-685` ; `DashboardTodayPanel.tsx:380-381`, `:396`, `:514` ; `audit/contrast_A.py` ; capture `avant/light-desktop/dashboard.png` | Encres de statut distinctes des aplats, chacune ≥ 4,5:1 dans les deux thèmes ; le texte de statut en neutre, la couleur sur un point. | nouveau (mesure) |
| A-53 | P2 | Transverse | Boutons : 89 `<button>` bruts et 5 `motion.button` contre 3 `<Button>` du kit dans le périmètre ; hauteurs 20, 24, 28, 32, 36 et 40 px ; rayons plein, 12 px, 8 px, 6 px, aucun ; le `Button` du kit est lui-même `rounded-full` avec des tailles 32/36/40/44. | `ui/button.tsx:9`, `:21-27` ; relevé `grep` sur les 63 fichiers du périmètre (coquille 6/3, barre 19/0, tableau de bord 16+5/0, tâches 17/0, agenda 31/0) | Réaligner `Button` (28/32/36/40 px, rayon 8 px, variantes primaire monochrome / secondaire / fantôme / destructive) puis migrer. | toujours vrai |
| A-54 | P3 | Transverse | Typographie hors échelle : 108 `text-[Npx]` dans le périmètre (barre 37, tableau de bord 30, agenda 21, tâches 18, coquille 2), 46 `font-display`, 46 `uppercase` ; le même sur-titre `text-[10px] uppercase tracking-wider` est recopié au moins 11 fois. | ex. `DashboardConnections.tsx:152`, `DashboardFocusPanel.tsx:260`, `DashboardWeekHighlight.tsx:152`, `:307`, `Tasks.tsx:144`, `CalendarDayView.tsx:105`, `CalendarListView.tsx:60`, `EventDetailSheet.tsx:214`, `:572` | Six paliers nommés ; sur-titre en casse normale 11 px semibold `text-muted-foreground`, via un composant `Eyebrow` ou la classe d'un `SectionHeader`. | toujours vrai |
| A-55 | P3 | Transverse | La tuile vert émeraude reste la signature hors jetons : 55 `emerald-*` dans le périmètre (agenda 20, tableau de bord 18, tâches 17) ; `IconTile`, fait pour ça, n'est utilisé dans aucun de ces écrans et son ton par défaut code lui-même l'émeraude. | `ui/IconTile.tsx:24` ; ex. `Tasks.tsx:268`, `Calendar.tsx:274`, `DashboardActivityFeed.tsx:199` | Supprimer la tuile d'en-tête (titre seul) ; ailleurs, tuile neutre `bg-muted`. | toujours vrai |
| A-56 | P2 | Transverse (structure) | Structure de page recopiée à la main : Tâches et Agenda reproduisent le même en-tête (tuile émeraude 40 px + `h1` `font-display` 24/30 px + sous-titre + barre de boutons `rounded-full`) au lieu de `PageHeader` ; le tableau de bord recopie quatre fois le même en-tête de carte (tuile, `h2` 15 px, lien « Voir tout ») et Tâches une cinquième. | `Tasks.tsx:261-325`, `Calendar.tsx:267-404` ; `DashboardMissionsPanel.tsx:308-352`, `DashboardTodayPanel.tsx:217-253`, `DashboardWeekHighlight.tsx:287-327`, `DashboardActivityFeed.tsx:197-218`, `Tasks.tsx:518-535` | `PageHeader` (titre, méta, actions) et `SectionCard` (titre, méta, action, corps) dans le registre cible. | toujours vrai |
| A-57 | P3 | Transverse (mouvement) | Trois animations d'entrée empilées à chaque arrivée sur une page : transition de route d'`AppLayout` (220 ms), `animate-in … slide-in-from-bottom-1 duration-300` de `PageLayout` (non couvert par la règle de mouvement réduit), puis l'en-tête et les blocs de la page (0,4 s, délais 0,05 à 0,1 s). | `AppLayout.tsx:51-56` ; `PageLayout.tsx:40` ; `Tasks.tsx:261-265` ; `Calendar.tsx:267-271`, `:407-411`, `:454-458` ; `index.css:450-457` | Garder la seule transition de route (déjà conforme) ; retirer les deux autres. | nouveau |

Bilan : 57 constats, dont 4 P1 (A-04, A-23, A-34, A-40), 30 P2 et 23 P3. Les constats A-18 à A-21 (et A-03 pour le bouton mobile, né du lot D37) sont à remettre à la session barre latérale.

### Précisions de méthode
- Les captures `avant/` et `audit/A-captures/` sont prises avec les polices de repli (Google Fonts injoignable depuis le bac à sable) : les jugements de famille et de graisse viennent du code, pas du rendu.
- Les pannes (A-34, A-40) et les bandeaux (A-04) sont simulés par interception réseau dans le navigateur (`capture-A.mjs`), sans aucune écriture en base.
- Chantier concurrent : pendant l'audit (à partir de 08:46 UTC), le chantier design (lots 1 et 2, en cours) a modifié sans les commiter 36 fichiers : `tailwind.config.ts`, `src/index.css`, `src/main.tsx`, `index.html`, une vingtaine de primitives `src/components/ui/*` (dont `button`, `dialog`, `sheet`, `IconTile`, `EmptyState`), les primitives `src/components/layout/*` (`EmptyState`, `PageHeader`, `PageLayout`, `Section`, `StatTile`) et un nouveau `src/lib/theme.ts` (`useAppTheme`). On y lit notamment : `darkMode` en variante hors `.light`, `font-display` et `serif` rendus en Instrument Sans, accent `--brand` indigo, `--ring` indigo, `MotionConfig reducedMotion="user"`, polices réduites à Instrument Sans, Space Mono et Bricolage, animation propre de `PageLayout` retirée. Toutes les captures de `audit/A-captures/` sauf `check-border.mjs` sont antérieures à ces modifications (08:48-08:52 UTC), et toutes les citations renvoient à `HEAD`. Si ce chantier est fusionné, il ferme ou réduit A-16 (anneau), A-17 (familles), A-51 (`dark:` morts), une partie de A-23 (transformations framer ; pas les boucles d'opacité), une partie de A-57 (animation de `PageLayout`) et touche A-14 et A-53 (primitives) ; `useAppTheme` pourrait servir A-05 une fois branché dans la barre et la palette. Rien de ce que j'ai vu ne touche A-50 (le `--border` sombre garde son alpha) ni les écrans du périmètre eux-mêmes.

## Par écran : les changements qui feraient passer au niveau cible

### Coquille (en-tête, gardes, erreurs, 404, aide)
1. Rendre les bandeaux (essai, crédits, hors ligne) dans `<main>` sous l'en-tête, via un emplacement de `AppLayout`, avec un seul composant `Banner` (ton, texte, action, fermeture) (A-04).
2. Trancher l'en-tête : le supprimer sur ordinateur, ou en faire l'en-tête de page (titre, fil d'Ariane mission, actions) ; bouton de navigation nommé en français, 44 px sur téléphone (A-01, A-02, A-03).
3. Un registre unique des pages (libellé, icône, route, raccourci) et un `useTheme()` partagé par la barre, le menu et la palette (A-05, A-06).
4. Trois primitives d'état dans le registre cible, `ErrorState`, `EmptyState`, `Spinner`, et les brancher sur `ErrorBoundary`, `SectionErrorBoundary`, `OrganizationGuard`, la 404 et les chargements de la coquille (A-08 à A-11, A-14).
5. Anneau de focus indigo à 3:1 minimum et règle `h1, h2, h3` retirée (A-16, A-17).

### Tableau de bord
1. Retirer tout le décor : dégradé, tache floue, emoji, halos, pings, soulèvements au survol, tuiles émeraude ; une seule entrée de 200 ms (A-22, A-23, A-55).
2. Une `SectionCard` neutre pour les quatre panneaux (titre 14-16 px, méta, un lien « Voir tout ») et un en-tête de page sobre : « Bonjour Camille » + date, sans emoji (A-56, A-32).
3. « Pour aujourd'hui » : cartes neutres, accent sur le seul chiffre, même total que « À traiter » de la barre, ou un autre nom (A-26, A-32).
4. États explicites par section : squelette à hauteur fixe, erreur avec « Réessayer », comme `SidebarSection` ; corriger les courbes (A-24, A-25).
5. Remplacer le glisser-déposer par un mode « Personnaliser » accessible au clavier, ou le retirer ; initiales neutres et plus aucun logo deviné chez des tiers (A-27, A-28).

### Tâches
1. Afficher l'erreur au lieu de « Zéro tâche en cours » (A-34).
2. Retirer la bande de cinq compteurs (les en-têtes de section les portent) et replier les suggestions sur une ligne neutre (A-36, A-37).
3. Passe de texte : vouvoiement, « Aucune tâche en cours », suggestions sans doublon ni « + » (A-35).
4. Primitives partagées avec l'Agenda : `SegmentedControl` (`aria-pressed`), `FilterBar` / `FilterPill`, `Button` ; libellés associés dans la modale (A-38, A-39, A-45).
5. Catégories sans emoji, intitulé de poste en texte simple, avatar sans logo déduit du poste (A-27, A-36).

### Agenda
1. Afficher l'erreur au lieu de « Pas d'événement cette semaine » (A-40).
2. Vue semaine : semaine calée sur le lundi (ou « 7 prochains jours »), pas de ligne « maintenant » hors grille horaire, jours vides compacts ; vue Liste par défaut sur téléphone (A-41, A-49).
3. Vue jour : carte compacte lisible sous 70 px, chevauchements en colonnes, lignes d'heures visibles (A-42, A-50).
4. Couleurs : types en neutre avec icône, un seul signal coloré (conflit), tours en texte ; une seule table partagée avec la fiche (A-44).
5. Nom « Agenda » partout, vouvoiement dans la modale, autocomplétion en vraie `Combobox`, raccourcis gardés comme `GoShortcuts` (A-06, A-46, A-48).

### Barre latérale (pour information, à remettre à sa session)
La structure est au niveau ; restent l'échelle typographique (A-18), une couleur brute (A-19), trois cibles tactiles (A-20) et la décision accent ou neutre pour les compteurs (A-21).

## Ce qui fonctionne bien et sert de modèle
- `SidebarSection` : les cinq états explicites (chargement `aria-busy` lu « Chargement », hors ligne, erreur avec « Réessayer », données peut-être anciennes, vide avec prochaine action). Sur la même panne simulée, c'est le seul endroit qui dit la vérité (`SidebarSection.tsx:158-208`, capture `tasks-error-simulated.png`). C'est le patron d'`ErrorState` / `EmptyState` à généraliser.
- Chiffres honnêtes : `null` n'est jamais affiché comme 0 (`todoCount`, « – » et « Indisponible » dans « Pour aujourd'hui », `DashboardFocusPanel.tsx:176-180`, `:217-218`) ; tâches en retard en gris, jamais en rouge (`SidebarBottomRow.tsx:8`, `:83`).
- Accessibilité de la barre : onglets à focus itinérant et flèches (`SidebarTabs.tsx:79-85`, `:121-131`), noms accessibles qui portent les compteurs, `aria-current`, cibles de 44 px sur téléphone (`SidebarRow.tsx:39`, `:122`), épingle `aria-pressed` + `aria-disabled` expliqué (`MissionNavRow.tsx:89-104`), initiales neutres sans image réseau (`MissionNavRow.tsx:119-124`).
- Retrait optimiste avec « Annuler » pendant 8 s plutôt qu'une confirmation (`ApprovalsSection.tsx:78-112`) ; bandeau hors ligne gris avec l'heure des données (`OfflineBanner.tsx:25-43`).
- `AppLayout` : lien d'évitement, transition de route de 220 ms en entrée seule qui respecte le mouvement réduit, chargement limité à la zone principale avec `role="status"` (`AppLayout.tsx:22-27`, `:38-40`, `:51-59`) ; `useCountUp` respecte aussi la préférence (`useCountUp.ts:46-51`).
- Raccourcis `G` puis lettre gardés (champ de saisie, menus, dialogues) et documentés dans une fenêtre dédiée (`GoShortcuts.tsx:21-60`, `KeyboardShortcutsDialog.tsx`).
- Actions principales déjà monochromes (`bg-primary` / `bg-foreground`), conformes à la cible (`NewMissionButton.tsx:28`, `Tasks.tsx:319`, `Calendar.tsx:397`).
- États vides filtrés utiles : compte de ce qui est masqué et bouton « Effacer les filtres » (`Calendar.tsx:667-690`, `Tasks.tsx:426-441`) ; suppression de tâche confirmée par `AlertDialog` en français (`Tasks.tsx:703-742`).
- Contenu du bandeau d'essai juste (`role="status"`, texte différent pour admin et non-admin, `TrialBanner.tsx:33-45`) : seul son placement est en cause.

## Composants partagés manquants (ou à réécrire) pour ce périmètre
- `ErrorState` : titre humain, phrase, « Réessayer », détails techniques repliés ; patron : `SidebarSection` (A-08, A-25, A-34, A-40).
- `EmptyState` réécrit (icône neutre, titre, phrase, une action) et `Skeleton` / `Spinner` avec `role="status"` et mouvement réduit (A-11, A-14, A-25).
- `Banner` unique (essai, crédits, hors ligne, panne), rendu dans `<main>` sous l'en-tête (A-04).
- `PageHeader` (titre, méta, actions, fil d'Ariane) et `SectionCard` (titre, méta, lien « Voir tout », corps) (A-01, A-56).
- `SegmentedControl` (`aria-pressed`), `FilterBar` / `FilterPill` / `ToggleChip` communs à Tâches et Agenda (A-38, A-45).
- `StatusDot` (point + texte neutre) et `CountBadge` (compteur, accent) pour remplacer les pastilles capitales colorées (A-26, A-31, A-52).
- `Avatar` à initiales neutres, rond pour une personne, carré pour une organisation, sans logo deviné chez des tiers (A-26, A-27).
- `Combobox` (cmdk) pour choisir un candidat ou une mission (A-46).
- `useTheme()` (état, stockage, classe) et registre des pages `NAV_PAGES` (libellé, icône, route, raccourci) (A-05, A-06).
- Le `Checkbox` du kit partout où une case est dessinée à la main (A-30).
