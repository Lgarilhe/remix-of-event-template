# Audit F : Paramètres, facturation, marketplace, pages publiques

> Revue du 25 septembre 2026, en lecture seule, sur `main` au commit `5882084`, avant les lots 1 et 2 du chantier design. Les captures citées viennent du banc visuel local (`docs/design/05-banc-visuel.md`) et ne sont pas versionnées. Les textes entre guillemets sont cités tels qu'ils apparaissent dans l'application. Statut de chaque constat : `docs/design/02-constats.md`.

Audit design et UX en lecture seule, 2026-09-25, sur `main` (HEAD `5882084`, identique à `origin/main`). Aucun fichier du dépôt modifié.

Statut : TERMINÉ (66 constats, 5 P1).

## Méthode
- Référence du 9 septembre : `origin/claude/app-details-exhaustifs-s6nf9t:docs/design-system/{07-reglages-facturation.md,08-public-auth-portails.md,README.md}` lus en entier. Chaque constat porte un statut : **toujours vrai**, **corrigé** ou **nouveau**.
- PR #245 : la commande `git diff origin/main...origin/design/uniformisation-anti-slop` échoue ici (clone superficiel : « no merge base »). La branche porte un seul commit propre à la PR (`a20221be`, 3 septembre, « uniformisation Pricing + EmptyMissionState ») ; son effet a été lu par `git diff a20221be~1 a20221be -- src/pages/Pricing.tsx` et comparé au `Pricing.tsx` actuel.
- Code lu : `src/pages/Settings.tsx`, tout `src/components/settings/**` (dont `shell/`), `src/lib/settingsRoutes.ts`, `src/components/billing/**`, la page et les composants marketplace, landing, auth, pricing, portails, profil public, pages légales, désinscription. Jetons : `src/index.css`, `tailwind.config.ts`, `index.html`, `src/main.tsx`, primitives `ui/button|card|input|select|badge|switch`.
- Captures réelles (données de démo) : `scratchpad/captures/avant/{dark-desktop,dark-mobile,light-desktop}/`. Elles sont rendues sans les polices web (police système de repli) : les constats typographiques reposent sur le code, les captures servent pour la mise en page, les couleurs, les états et le responsive.
- Étalon : registre « calm premium » (Linear, Qonto) : neutres chauds teinte 40, bouton principal monochrome, un accent indigo désaturé rationné aux signaux, aucun effet décoratif, Instrument Sans 500/600/700 seule dans l'app, 6 paliers de texte, contrôles 28/32/36/40, rayons 8/12/plein, états explicites, vouvoiement, pages publiques de la même famille que l'app.

**Priorités** : P1 = information fausse ou parcours cassé, règle de marque violée, contraste ou accessibilité bloquants, effet décoratif sur l'action principale. P2 = incohérence visible, état manquant ou mensonger, accessibilité non bloquante, écart net au registre. P3 = finition.

**Destinataires** : « À remettre à la session Paramètres » (la session qui poursuit les lots de `/settings`), « Public » (landing, auth, pricing, légal), « Portails » (candidat, client, profil recruteur), « Marketplace », « Transverse » (jetons globaux, hors périmètre propre mais visibles ici).

## Avancement
- [x] Lecture matière existante (07, 08, README du 9 septembre)
- [x] Jetons globaux (index.css, tailwind.config.ts, index.html, main.tsx)
- [x] /settings (coquille + 7 rubriques) et billing
- [x] marketplace
- [x] landing
- [x] auth
- [x] pricing (+ PR #245)
- [x] portails (candidat, client, profil public recruteur)
- [x] privacy, privacy-extension, unsubscribe

**Arbre de travail partagé** : pendant l'audit, le chantier design (lots 1 et 2) a modifié sans commit `index.html`, `src/index.css`, `src/main.tsx`, `tailwind.config.ts`, `package.json`, `.github/workflows/ci.yml`, puis les primitives du kit (`ui/button`, `card`, `input`, `select`, `badge`, `switch`, `tabs`, `dialog`, `sheet`, `alert-dialog`, `IconTile`, `EmptyState`…) et de mise en page (`components/layout/*`), et ajouté `docs/design/01-direction.md`, `scripts/design/ratchet.mjs`, `src/lib/theme.ts` (`git status --short`). Aucun écran du périmètre n'était modifié à la clôture. Toutes les preuves de ce rapport visent le code commité (`HEAD 5882084`) ; les références aux fichiers globaux et au kit ont été revérifiées par `git show HEAD:…`. La direction en cours d'écriture (`docs/design/01-direction.md`) reprend l'étalon de cet audit : accent indigo nommé `brand`, Instrument Sans seule dans l'app, Bricolage Grotesque réservée aux titres publics, Outfit, Space Grotesk et Instrument Serif retirés, rayons 8/12/plein, capitales réservées à la classe `eyebrow`. Les constats transverses F-04, F-29, F-30, F-32 et F-38 (et le contexte « Kit » ci-dessous) relèvent de ce chantier ; ils restent ouverts tant qu'il n'est pas commité et que les écrans n'ont pas migré.

## Contexte transverse constaté (sert de preuve à plusieurs constats)
- Thème : `:root` = sombre (`src/index.css:12-128`), `.light` = clair (`:131-214`). Le choix vient de la barre latérale (`src/components/AppSidebar.tsx:76-85`, clé `konekt-theme`) et est rejoué avant rendu (`src/main.tsx:105-113`). `tailwind.config.ts:4` déclare `darkMode: ["class"]` mais aucune classe `.dark` n'est jamais posée : toutes les variantes `dark:` sont mortes.
- Polices chargées (`index.html:22-25`) : Space Grotesk (aucun usage dans `src/`), Space Mono, Outfit 400-900, Instrument Serif, Instrument Sans, Bricolage Grotesque. `h1, h2, h3` forcés en Outfit 700 (`src/index.css:241-244`).
- Accent : `--accent` shadcn = gris (`src/index.css:32`, `:150`). L'indigo `--k-accent` (`:107`, `:187`) n'existe que dans la recherche ; aucune page de ce lot ne l'emploie.
- `--success` n'existe pas : seul `--status-success` est défini (`src/index.css:67`), mappé sur `success` dans `tailwind.config.ts:106-110`.
- Kit : `Button` en `rounded-full`, `h-10` par défaut, `sm` = `h-9`, `xs` = `h-8`, `lg` = `h-11` (`src/components/ui/button.tsx:9-26`) ; `Card` `rounded-xl` = 16 px (`ui/card.tsx:6`, `--radius: 0.75rem` + 4 px) ; `Input` et `SelectTrigger` `h-10 rounded-lg` = 12 px. Aucun des trois rayons cibles (8/12/plein) n'est tenu par le kit pour les champs (12 au lieu de 8) ni pour les cartes (16 au lieu de 12).

## Tableau des constats

| ID | Prio | Écran | Constat | Preuve | Correctif | Statut vs 9 sept. | Destinataire |
|----|------|-------|---------|--------|-----------|-------------------|--------------|
| F-01 | P2 | /settings (toutes rubriques) | Les rubriques partagent désormais la même anatomie d'en-tête (titre + phrase d'intro), mais les cartes à l'intérieur gardent 4 conventions de titre : `text-lg` gras (rubrique Connexions), `text-sm font-bold uppercase tracking-wider` (Général, Équipe, Rédaction, Abonnement, Règles), `h3 text-sm font-semibold` (Notion, politiques, connecteurs, intégrations), `h3 text-base font-bold` (enrichissement). Visible d'une rubrique à l'autre : « Mon compte LinkedIn » en gros gras contre « MON CONTEXTE IA » en capitales espacées. | `MyLinkedInAccount.tsx:322`, `:1007` ; `LinkedInSafetySettings.tsx:116` ; `MyEmailAccount.tsx:163` ; `ExtensionTokens.tsx:159` ; `GeneralSection.tsx:54` ; `TeamSection.tsx:77` ; `AiContextSettings.tsx:54`, `:92` ; `BillingSettings.tsx:185`, `:199`, `:303`, `:326` ; `NotionConnectionCard.tsx:130` ; `AgentPoliciesSettings.tsx:128` ; `IntegrationsSettings.tsx:195` ; `EnrichmentAnalytics.tsx:207`. Captures `dark-desktop/settings-connections.png` et `settings-writing.png` | Un seul titre de carte : `text-sm font-semibold`, casse de phrase, icône 16 px en `text-muted-foreground` facultative ; action de carte dans un emplacement fixe à droite du titre. | toujours vrai (anomalie B) | À remettre à la session Paramètres |
| F-02 | P2 | /settings/account/journal | Double titre : la coquille affiche le h2 « Journal de l'assistant » et sa phrase, puis la rubrique rend un second h2 « Actions IA » avec icône et une seconde phrase. Deux noms pour la même chose, deux intros empilées ; « mis à jour en temps réel » à côté d'un bouton « Rafraîchir ». | `Settings.tsx:177-178` ; `AgentActionsSettings.tsx:417-436`. Captures `dark-desktop/settings-journal.png`, `dark-mobile/settings-journal.png` | Retirer l'en-tête interne ; « Rafraîchir » passe dans la barre de filtres (ou disparaît, le canal temps réel de `:243-261` suffit). | toujours vrai, devenu doublon avec la coquille | À remettre à la session Paramètres |
| F-03 | P2 | /settings (rail) | La rubrique active est un aplat inversé (pastille blanche en sombre, noire en clair), alors que la barre latérale de l'app marque l'élément actif par un fond discret. Deux règles de sélection côte à côte sur le même écran. | `Settings.tsx:116` (`bg-foreground text-background`) contre `sidebar/SidebarTabs.tsx:139` et `sidebar/SidebarBottomRow.tsx:75` (`bg-sidebar-accent`). Captures `dark-desktop/settings-*.png`, `light-desktop/settings-billing.png` | Même traitement que la barre latérale (fond `muted`, texte `foreground`, poids 600) ; l'inversion reste réservée au bouton principal. | toujours vrai (pastille `bg-foreground`, sans l'animation framer d'alors) | À remettre à la session Paramètres |
| F-04 | P2 | /settings | Trois familles visibles : « Paramètres », le titre de rubrique et chaque `CardTitle` (h3) sont en Outfit via la règle globale, le corps en Instrument Sans, jetons et raccourcis en Space Mono. | `src/index.css:241-244` ; `ui/card.tsx:19` (CardTitle = h3) ; `Settings.tsx:59`, `:135`, `:177` ; `font-mono` dans `ExtensionTokens.tsx:190`, `:243`, `MessageTemplatesSettings.tsx:193` | Retirer la règle `h1, h2, h3` ; titres en Instrument Sans 600 (rubrique) et 700 (page). Mono seulement pour un secret ou un code à copier. | toujours vrai | Transverse (visible dans Paramètres) |
| F-05 | P2 | /settings | 71 tailles en pixels hors échelle (46 × `text-[10px]`, 21 × `[11px]`, 3 × `[13px]`, 1 × `[12px]`) et 37 `uppercase` dans la coquille et les rubriques, alors que `tailwind.config.ts:143-149` dit bannir `text-[Npx]`. Les libellés de groupe du rail sont eux-mêmes en `text-[11px]` capitales. | `grep -rhoE "text-\[[0-9.]+px\]" src/pages/Settings.tsx src/components/settings` ; `Settings.tsx:84` ; `AiContextSettings.tsx:177`, `:208`, `:216`, `:333` ; `AgentPoliciesSettings.tsx:145`, `:146`, `:166`, `:171` | Six paliers nommés ; les compteurs et aides passent au palier 11-12 px ; capitales réservées à un seul usage (libellé de groupe du rail, s'il en faut). | toujours vrai | À remettre à la session Paramètres |
| F-06 | P2 | /settings (Journal, Règles, Connexions) | Faux vides : une lecture ratée s'affiche comme un état vide ou un état par défaut. Journal : « Aucune action ne correspond à ces filtres. » ; ICP : « Aucun ICP configuré » ; e-mail : écran « Connectez votre compte email » ; politiques : chaque action affiche « Avec approbation » (valeur par défaut d'une Map vide). D'autres blocs sont passés à `ErrorBox` depuis le 9 septembre (abonnement, plages LinkedIn, contexte IA, modèles, signatures, statistiques d'enrichissement, liaisons LinkedIn de l'équipe), ceux-ci non. | `AgentActionsSettings.tsx:211-215` (seul `isLoading` lu) et `:498-504` ; `hooks/usePedigreePresets.ts:33-36` (erreur en `console.error`) et `PedigreePresetsSettings.tsx:88-96` ; `MyEmailAccount.tsx:50-52` ; `AgentPoliciesSettings.tsx:79-95` et `:162` | `ErrorBox` + « Réessayer » dans ces quatre blocs, comme `BillingSettings.tsx:180-194` ou `LinkedInSafetySettings.tsx:149-156`. | nouveau (le 9 septembre ne relevait que les chargements) ; corrigé ailleurs (voir « Corrigé depuis le 9 septembre ») | À remettre à la session Paramètres |
| F-07 | P2 | /settings/account/connections | Statut qui ne dit pas la vérité : quand la liste des comptes ne donne pas de statut, l'état est « inconnu » (pastille et texte orange, bouton « Reconnecter » proposé) mais le libellé est tiré du statut enregistré sur la liaison et affiche « Actif ». La capture montre « ● Actif » en orange à côté de « Reconnecter ». | `lib/linkedinStatus.ts:70` (état calculé sur `account.status`) et `:75` (libellé repli sur `mapping.account_status`) ; `MyLinkedInAccount.tsx:364-373`, `:379-390`. Capture `dark-desktop/settings-connections.png` | Calculer libellé et couleur sur la même valeur : « État à vérifier » quand l'état est inconnu (le libellé existe déjà, `MyLinkedInAccount.tsx:624-626`). | nouveau | À remettre à la session Paramètres |
| F-08 | P2 | /settings/account/connections (mobile) | La ligne du compte LinkedIn ne se replie pas : à 390 px, l'avatar et le nom passent sous « Reconnecter » ; « Actif » chevauche le bouton. | `MyLinkedInAccount.tsx:346-420` (`flex justify-between`, actions `shrink-0`, aucun `flex-wrap`). Capture `dark-mobile/settings-connections.png` | Sous `sm`, actions sous l'identité (`flex-col` ou `flex-wrap`), boutons pleine largeur. | nouveau | À remettre à la session Paramètres |
| F-09 | P2 | /settings (toutes rubriques) | Enregistrement sans règle lisible, sur le même écran : **immédiat sans confirmation** pour le type d'organisation (qui change droits et rubriques en un clic), le rôle d'un membre, les politiques et le résumé de l'assistant ; **bouton + indicateur** « Modifications non enregistrées / À jour » pour le contexte IA ; **bouton sans indicateur** pour les plages LinkedIn ; **édition en place** avec « Sauver » et un « ✓ » littéral. Quatre verbes : Sauver, Sauvegarder, Enregistrer, Mettre à jour. | `OrgTypeSetting.tsx:30-50`, `:59-73` ; `TeamManagement.tsx:226` ; `AgentPoliciesSettings.tsx:153-157`, `:177` ; `AiContextSettings.tsx:331-359` ; `LinkedInSafetySettings.tsx:247-256` ; `GeneralSection.tsx:83-86` ; `OrgLogoEditor.tsx:250-258` ; `EmailSignatures.tsx:186` ; `TeamManagement.tsx:424` | Règle unique : réglage atomique et réversible = enregistrement immédiat + mention « Enregistré » en ligne ; formulaire = bouton « Enregistrer » + indicateur d'état ; changement structurant (type d'organisation, rôle) = confirmation qui dit la conséquence. Un seul verbe : « Enregistrer ». | toujours vrai pour les verbes et le « ✓ » ; nouveau pour la règle | À remettre à la session Paramètres |
| F-10 | P1 | /settings/account/connections (extension) | Le dialogue « Installer l'extension Chrome Konekt » demande au client d'ouvrir PowerShell dans le dossier d'un développeur (`cd C:\Users\Hugo\dev\remix-of-event-template\extensions\chrome`, `npm install`, `npm run build`), le tout au tutoiement. Parcours impossible pour un client. | `ExtensionTokens.tsx:354-417` (chemin `:367-370`) | Remplacer par la procédure client (lien de téléchargement ou « bientôt disponible ») ; sinon masquer le bouton hors comptes internes. | toujours vrai (anomalie F) | À remettre à la session Paramètres |
| F-11 | P2 | /settings (Connexions, Journal) | Tutoiement résiduel dans une app au vouvoiement : carte et messages Notion, confirmation d'action sensible du Journal, dialogue d'installation de l'extension. | `NotionConnectionCard.tsx:39-44`, `:70`, `:148`, `:192`, `:217` ; `AgentActionsSettings.tsx:346`, `:529` ; `ExtensionTokens.tsx:365`, `:380-393`, `:402` | Vouvoiement partout. | toujours vrai | À remettre à la session Paramètres |
| F-12 | P2 | /settings (Rédaction) | Actions « Modifier » et « Supprimer » visibles au survol seulement : invisibles au toucher et quand le focus clavier arrive dessus. | `MessageTemplatesSettings.tsx:242` ; `EmailSignatures.tsx:101` | Toujours visibles en `text-muted-foreground`, ou `group-focus-within:opacity-100` + visibles sous `md`. | toujours vrai | À remettre à la session Paramètres |
| F-13 | P2 | /settings/org/assistant (ICP) | Couleurs brutes et contraste : encart RGPD en `text-amber-700 dark:text-amber-400` : la variante `dark:` est morte, le texte reste ambre foncé sur carte sombre (≈ 3,1:1 en 12 px, calcul `#b45309` sur `hsl(40 3% 14%)`). Badges « Par défaut » en émeraude et « Mode strict » en ambre bruts. | `PedigreePresetsSettings.tsx:72-81`, `:153`, `:197` ; `tailwind.config.ts:4` ; `main.tsx:105-113` | Jetons `warning` et `success` (ou neutre) ; supprimer `dark:`. | toujours vrai (anomalie E) ; la variante morte est nouvelle | À remettre à la session Paramètres |
| F-14 | P2 | /settings (Abonnement, Général, Équipe, Connexions) | Couleurs de signal employées hors signal : chaque consommation de crédits s'affiche en rouge destructif ; badges pleins verts « 1 compte connecté » et « Connecté » ; barres de plafond LinkedIn au bleu de la marque LinkedIn ; « EXTERNE » en bleu info capitales ; encart de protections en vert. La couleur ne signale plus rien de particulier. | `AICreditsSettings.tsx:359` ; `IntegrationsSettings.tsx:208`, `:442` ; `NotionConnectionCard.tsx:137` ; `MyLinkedInAccount.tsx:971` ; `TeamManagement.tsx:194` ; `LinkedInSafetySettings.tsx:122` ; tuile blanche en dur du logo Notion, invisible en sombre (`NotionConnectionCard.tsx:124`) ; badges d'invitation en classes ad hoc, « En attente » en `accent` gris (`PendingInvitations.tsx:39-63`). Captures `settings-general.png`, `settings-team.png` | Neutre par défaut (texte `foreground`, pastille 6 px pour l'état) ; couleur réservée aux écarts (alerte, échec, épuisé) ; consommation en `foreground`, recharge en `success`. | nouveau (angle registre) | À remettre à la session Paramètres |
| F-15 | P2 | /settings (formulaires) | Accessibilité : libellés non reliés à leur champ (`Label`/`label` sans `htmlFor`) ; boutons icône sans nom (rafraîchir l'e-mail, supprimer un compte d'intégration, modifier/supprimer un ICP) ; interrupteur du résumé sans nom ; type d'organisation en boutons sans `aria-pressed` ni `radiogroup` ; en-tête d'intégration en `<button>` sans `type` ni `aria-expanded`. | `AiContextSettings.tsx:163`, `:194`, `:215`, `:264`, `:313` ; `InviteMemberForm.tsx:40`, `:51` ; `EmailSignatures.tsx:152`, `:161` ; `GeneralSection.tsx:73` ; `OrgLogoEditor.tsx:234` ; `MyEmailAccount.tsx:193-200`, `:213-220` ; `IntegrationsSettings.tsx:184-185`, `:422-423`, `:262-273` ; `PedigreePresetsSettings.tsx:161`, `:166` ; `AgentPoliciesSettings.tsx:153-157` ; `OrgTypeSetting.tsx:59-73` ; bascule « Inclure / Masquer les lectures » sans `aria-pressed` (`AgentActionsSettings.tsx:488-495`) | `htmlFor`/`id` partout ; `aria-label` sur les boutons icône ; `RadioGroup` (ou `aria-pressed`) pour le type ; `aria-expanded` + `type="button"` sur l'en-tête repliable. | toujours vrai (anomalie I) + nouveaux cas | À remettre à la session Paramètres |
| F-16 | P2 | /settings/org/team | Libellés tronqués : le sélecteur de rôle (`w-28`) coupe « Collaborateur » (« Collaborateu ») ; sur mobile, nom et e-mail coupés à 8 caractères, « Pas de LinkedIn » sur deux lignes. | `TeamManagement.tsx:228`. Captures `dark-desktop/settings-team.png`, `dark-mobile/settings-team.png` | Largeur au contenu (`w-auto min-w-28`) ; sous `sm`, sélecteur et corbeille sous l'identité. | nouveau | À remettre à la session Paramètres |
| F-17 | P3 | /settings/account/writing et /org/assistant | Le sélecteur « Ton » affiche « Auto… » décalé : chaque option contient un bloc de deux lignes (libellé + aide) que le déclencheur tronque. | `AiContextSettings.tsx:170-187`. Captures `settings-writing.png`, `settings-assistant.png` | Déclencheur = libellé seul ; aide sous le champ ou dans la liste uniquement. | nouveau | À remettre à la session Paramètres |
| F-18 | P3 | /settings | Surfaces carrées dans des cartes arrondies (reliquat « brutal ») : vignette du logo, lignes de signatures, jetons d'extension et bloc « Token créé ! », badge de pack de crédits ; avatars des membres carrés quand ils sont ronds partout ailleurs. Boutons « CHANGER LE LOGO » / « SUPPRIMER » en capitales. | `OrgLogoEditor.tsx:165`, `:191`, `:204` ; `EmailSignatures.tsx:90` ; `ExtensionTokens.tsx:179`, `:228`, `:239` ; `AICreditsSettings.tsx:240-244` ; `TeamManagement.tsx:182`. Capture `settings-general.png` | Rayon 8 px pour les sous-surfaces, avatar rond, boutons en casse de phrase. | toujours vrai | À remettre à la session Paramètres |
| F-19 | P3 | /settings | Emojis et glyphes servant d'icônes : modèles suggérés (👋 ⏰ 📅 🙏 💼 🗓), catégories de variables (👤 ✨ 🎯 ✍️ 📅 💬), 💡, 🧩, ⚠️ en tête d'avertissement. | `MessageTemplatesSettings.tsx:40-108`, `:330-335`, `:454`, `:623` ; `ExtensionTokens.tsx:185`, `:384` ; `MyLinkedInAccount.tsx:721`, `:776` ; `AgentActionsSettings.tsx:538`, `:644` | Icônes lucide ou rien. | toujours vrai | À remettre à la session Paramètres |
| F-20 | P3 | /settings | Vocabulaire : l'assistant s'appelle tour à tour « IA Konekt », « assistant IA », « l'assistant », « Actions IA », « chat IA » ; « agence » pour toute organisation (« Contexte IA agence », « Comptes LinkedIn de l'agence ») ; anglicismes à l'écran : user, do/don't, Patterns, Templates, composer, sender, Cap, capez, reset, tokens, c€ ; « Collaborateur » contre « Collaborateur externe » ; sous-titre « Invitations » dans la carte « Invitations ». | `AiContextSettings.tsx:62-64`, `:94`, `:102-104`, `:217` ; `AgentActionsSettings.tsx:421` ; `IntegrationsSettings.tsx:90` ; `MessageTemplatesSettings.tsx:145`, `:160`, `:333` ; `LinkedInSafetySettings.tsx:140`, `:215` ; `AICreditsSettings.tsx:252`, `:281` ; `TeamManagement.tsx:232-234` contre `InviteMemberForm.tsx:59` ; `PendingInvitations.tsx:111-114` ; « tous les missions » et « données de l'agence » (`TeamManagement.tsx:465`) ; badge « désactivé » en minuscules (`AgentConnectorsSettings.tsx:319`) ; cartes « Exécutée » au singulier, filtre « Exécutées » au pluriel (`AgentActionsSettings.tsx:152`, `:468`) ; toast mi-anglais « Action approve a échoué » (`AgentActionsSettings.tsx:297`) ; marque tierce en exemple (« ex: BlaBlaCar », `PedigreePresetsSettings.tsx:279`, `:298`) | Un nom (« l'assistant ») ; « organisation » ; glossaire FR ; même libellé de rôle partout ; retirer le sous-titre doublon. | toujours vrai (partiellement) | À remettre à la session Paramètres |
| F-21 | P3 | /settings/org/team, /account/connections | Suppressions sans confirmation : annuler une invitation, dissocier l'adresse e-mail. | `PendingInvitations.tsx:175` ; `MyEmailAccount.tsx:202`, `:222` | `AlertDialog` comme pour LinkedIn. | toujours vrai (le logo et les connecteurs sont corrigés) | À remettre à la session Paramètres |
| F-22 | P3 | /settings (Général, Rédaction, Abonnement) | Icônes à contresens : crayon du site web = immeuble (`Building2`) ; étincelles (signe IA) sur « Enregistrer », « Limites du plan », « Enrichissement de contact », « Recharges ». | `OrgLogoEditor.tsx:274` ; `AiContextSettings.tsx:355` ; `BillingSettings.tsx:304` ; `EnrichmentAnalytics.tsx:206` ; `AICreditsSettings.tsx:189` | `Pencil` ; étincelles réservées à une génération IA. | toujours vrai (Building2) ; nouveau (étincelles) | À remettre à la session Paramètres |
| F-23 | P3 | /settings/org/billing, /org/general | Rangement : « Export des données (RGPD) » et l'interrupteur de la source « Base Konekt » vivent dans « Abonnement et crédits » ; les intégrations de Général n'ont pas de titre (ancre `#outils` sans en-tête). « Limites du plan » reste sans consommation, en `grid-cols-2` fixe. | `BillingSettings.tsx:323-345`, `:306-315` ; `AICreditsSettings.tsx:271` ; `GeneralSection.tsx:112` | Export dans Général ; source de recherche dans Règles de l'assistant ou Général ; titre « Outils reliés » ; jauges d'usage dans « Limites du plan ». | toujours vrai (limites) ; nouveau (rangement) | À remettre à la session Paramètres |
| F-24 | P3 | /settings/org/general | « + Ajouter une intégration » : `+` littéral, bordure pointillée 2 px, menu absolu fait main sans clavier, sans Échap ni clic extérieur. Tuile du logo compressée sur mobile. | `IntegrationsSettings.tsx:584-608`, `:190-192`. Capture `dark-mobile/settings-general.png` | `DropdownMenu` du kit, icône `Plus`, bordure pleine 1 px ; tuile `shrink-0`. | toujours vrai | À remettre à la session Paramètres |
| F-25 | P3 | /settings/account/connections | Réglages techniques exposés tels quels : fuseaux en identifiants IANA (« America/New_York ») ; statuts bruts du fournisseur d'e-mail et des comptes de l'intégration LinkedIn (« Jamais le code brut à l'écran » n'est appliqué qu'à la carte LinkedIn personnelle). | `LinkedInSafetySettings.tsx:203-208` ; `MyEmailAccount.tsx:185-188` ; `IntegrationsSettings.tsx:256` | Libellés FR (« Paris (UTC+1) ») ; `statusLabel` partagé. | toujours vrai | À remettre à la session Paramètres |
| F-26 | P2 | /marketplace | Deux dialectes sur la même page : la carte « Cercle partenaires » et la liste « Missions partenaires » sont arrondies (`rounded-xl`, la seconde se soulève au survol), les onglets, cartes de mission, listes de candidatures, missions d'entreprise et l'état « type manquant » sont carrés, en capitales, avec aplats inversés. Un partenaire suspendu voit les deux empilés. | `PartnerCircleCard.tsx:150` ; `PartnerMissionsSection.tsx:61` ; `PartnerMarketplace.tsx:50-54`, `:184`, `:327`, `:421-434` ; `EnterpriseHuntMissions.tsx:24-60`, `:93-102` ; `Marketplace.tsx:49-57`, `:68-83` | Une anatomie : `Card` du kit (rayon 12), titres en casse de phrase, tags en `Badge` neutre, survol par changement de filet sans déplacement. | toujours vrai (02, §5.4 et synthèse l. 621) | Marketplace |
| F-27 | P2 | /marketplace | Hiérarchie d'action : chaque carte de mission porte un bouton plein inversé en capitales (« POSTULER », « GÉRER », « OUVRIR LA MISSION ») et chaque compétence est une étiquette inversée ; une grille de 9 missions affiche 9 actions principales et des dizaines d'aplats. Le dialogue « Postuler » (kit, arrondi) se termine par deux boutons natifs carrés en capitales. | `PartnerMarketplace.tsx:206`, `:244`, `:286-303`, `:363` ; `EnterpriseHuntMissions.tsx:58` | Action de carte en `Button variant="outline" size="sm"` ; un seul bouton plein par vue ; compétences en texte ou `Badge` discret ; `DialogFooter` avec `Button`. | toujours vrai | Marketplace |
| F-28 | P2 | /marketplace | Accessibilité : recherche et deux listes déroulantes natives en `focus:outline-none` sans remplacement (le contour global `:focus-visible` est annulé) ; onglets faits main sans `role="tab"` ni `aria-selected` ; libellés de formulaire (`FieldLabel`) non reliés aux champs, en 10 px capitales. | `PartnerMarketplace.tsx:133`, `:139`, `:149`, `:452-468`, `:267` ; `PartnerCircleCard.tsx:29-31` ; `src/index.css:223-226` | `Input`, `Select`, `Tabs` du kit ; `Label htmlFor`. | toujours vrai (02, l. 665-666) | Marketplace |
| F-29 | P3 | /marketplace | Deux chargements différents : carré qui tourne sans `rounded-full` (page, onglets, missions d'entreprise) et `Loader2` (carte partenaire, panneau d'administration) ; aucun squelette. États vides recopiés trois fois ; `EmptyState` et `PageHeader` du kit jamais importés (`PageLayout` recopié à la main). | `Marketplace.tsx:42`, `:88-92` ; `PartnerMarketplace.tsx:43-46`, `:49-55` ; `EnterpriseHuntMissions.tsx:84`, `:93-99` ; `PartnerCircleCard.tsx:122` ; `components/layout/PageHeader.tsx` (aucun import) | Squelette de cartes pour les listes, `EmptyState` et `PageHeader`/`PageLayout` du kit. | toujours vrai | Marketplace |
| F-30 | P3 | /marketplace | En-tête de page propre à la page (icône 24 px + h1 + sous-titre en capitales espacées) : ni celui des Paramètres, ni celui du Pipeline ou de l'Agenda. | `Marketplace.tsx:93-101` ; comparer `Settings.tsx:59`, `ATS.tsx:263`, `Calendar.tsx:278`, `Agents.tsx:69` | `PageHeader` unique pour toutes les pages de l'app, sous-titre en casse de phrase. | toujours vrai | Transverse (Marketplace) |
| F-31 | P3 | /marketplace | Contenu : adresse personnelle en dur (`mailto:l.garilhe@konekt.fr`) pour contester une suspension ; « ci dessous » sans trait d'union ; tuile d'icône émeraude « signature » (couleur hors jetons, `IconTile` par défaut). | `PartnerCircleCard.tsx:200`, `:212`, `:152` ; `ui/IconTile.tsx:11`, `:24` | Adresse d'équipe issue d'une constante ; « ci-dessous » ; tuile neutre (`bg-muted`). | toujours vrai | Marketplace |
| F-32 | P3 | Transverse (Paramètres, Marketplace, Missions) | `ErrorBox` est devenu l'état d'erreur commun (11 fichiers) mais reste un bloc carré (`border … p-6`, sans rayon) rangé dans `components/marketplace/` ; posé dans les cartes arrondies des Paramètres, il réintroduit l'angle droit. | `components/marketplace/ErrorBox.tsx:20` ; importeurs : `BillingSettings.tsx`, `TeamManagement.tsx`, `LinkedInSafetySettings.tsx`, `AiContextSettings.tsx`, `EmailSignatures.tsx`, `MessageTemplatesSettings.tsx`, `EnrichmentAnalytics.tsx`, `MissionHuntMode.tsx` | Déplacer vers `components/ui/` (futur `ErrorState`), rayon 8, même famille que `EmptyState`. | nouveau (usage étendu depuis le 9 sept.) | Transverse |
| F-33 | P1 | / (landing) | Les quatre « Réserver une démo » / « Démo » (nav, hero, tiroir mobile, CTA final) ouvrent une iframe sur une adresse de démonstration générique d'agenda (`https://calendly.com/demo/30min`) : aucun rendez-vous Konekt n'est possible depuis le CTA principal du hero. | `SkalrLanding.tsx:51`, `:203`, `:280`, `:344`, `:607`, `:652` | Vraie adresse de prise de rendez-vous en constante d'environnement, sinon le formulaire de contact. | toujours vrai (08, B.14) | Public |
| F-34 | P1 | /auth, /pricing | Le logo est appelé en bleu marine (`theme="dark"`) sur un fond presque noir : peu lisible sur les deux pages d'entrée (captures). Le composant ne suit pas le thème ; la barre latérale, elle, choisit la variante selon le thème. | `Auth.tsx:303` ; `Pricing.tsx:303` ; `KonektLogo.tsx:35-44` ; comparer `AppSidebar.tsx:224-229`. Captures `dark-desktop/auth.png`, `dark-desktop/pricing.png`, `dark-mobile/pricing.png` | Logo sensible au thème (variante blanche en sombre), par défaut dans `KonektLogo` ; ou fichier SVG en `currentColor`. | toujours vrai (08, A.7) ; visible sur capture | Public |
| F-35 | P2 | / (landing) | Thème clair imposé par effet de bord (`classList.add('light')`) quel que soit le choix du visiteur ; passer de `/` à `/auth` ou `/pricing` fait basculer clair → sombre. Les pages publiques n'ont pas de thème commun. | `SkalrLanding.tsx:83-94`. Captures `dark-desktop/landing.png` (claire) puis `dark-desktop/auth.png` (sombre) | Un seul thème pour tout le parcours public (celui du visiteur, sombre par défaut comme l'app, ou préférence système), porté par les jetons ; supprimer le forçage. | toujours vrai (08, A.1) | Public |
| F-36 | P2 | / (landing) | Troisième identité visuelle, « brutale » : `BrutalButton` (angles droits, bordure 2 px, capitales, ombre au survol, pas de `disabled`), cartes `border-2`, eyebrows, titres de valeurs et questions de FAQ en capitales ; l'app utilise des boutons pilule et des cartes arrondies. | `SkalrLanding.tsx:54-79`, `:312`, `:415`, `:420`, `:466`, `:471`, `:504`, `:559-566`, `:616` | `Button` du kit, cartes rayon 12, casse de phrase ; les capitales réservées à un eyebrow au plus. | toujours vrai (08, A.2-3) | Public |
| F-37 | P2 | / (landing) | Effets décoratifs : fonds en dégradé ciel (hero, CTA final), carte visuelle en dégradé + pastille jaune « Match IA », filet dégradé violet-rose qui balaie la démo, ombre au survol sur des cartes non cliquables (valeurs, chiffres). | `SkalrLanding.tsx:303`, `:391-398`, `:466`, `:504`, `:588` ; `LandingProductDemo.tsx:144-151` ; `src/index.css:302-308` (`landing-sky-gradient`), `:80` (`--landing-accent-yellow`) | Fonds unis (`background`, `card`) ; pas d'ombre sur l'inerte ; l'accent indigo pour un seul signal (le score). | toujours vrai | Public |
| F-38 | P2 | / (landing) | Cinq familles sur une page : Bricolage Grotesque (h1), Instrument Serif italique (mot « simplifié », tous les h2, citation), Outfit (h3 via la règle globale), Space Mono (numéros 001-004, chrome de la démo), Instrument Sans (corps). Space Grotesk est chargée sans aucun usage. | `SkalrLanding.tsx:322`, `:325`, `:377`, `:421`, `:452`, `:528` ; `src/index.css:241-244` ; `index.html:22-25` | Une seule police de titrage de marque (réservée au public) + Instrument Sans ; retirer Space Grotesk, Space Mono et Outfit du public. | toujours vrai (08, §0 et 37) | Public |
| F-39 | P2 | / (landing) | Preuves non sourcées : « ×3 », « −60% », « +80% » et une citation anonyme (« Head of Talent, Scale-up Tech », avatar « T »). Dans un registre sobre, un chiffre sans source affaiblit la page. | `SkalrLanding.tsx:146`, `:151-155`, `:517-543` | Retirer, ou sourcer (étude datée, client nommé avec accord). | toujours vrai (08, 1.8) | Public |
| F-40 | P2 | / (landing) | Surcouches inaccessibles : modales démo et contact, tiroir mobile = `div` sans `role="dialog"`, sans piège de focus ni Échap ; boutons « X » sans nom ; FAQ sans `aria-expanded` ; libellés du formulaire non reliés ; bouton « Envoyer » cliquable pendant l'envoi. | `SkalrLanding.tsx:225-300`, `:630-711` (X `:646-651`, `:675-680`), `:562-568`, `:688-702`, `:704-706` | `Dialog`, `Sheet`, `Accordion`, `Label htmlFor`, `Button disabled` du kit. | toujours vrai (08, E.26, E.29) | Public |
| F-41 | P2 | / (landing) | La démo produit tourne en boucle (cycle de 11 s) sans pause ni arrêt hors écran : mouvement automatique de plus de 5 s sans moyen de l'arrêter (WCAG 2.2.2) ; `prefers-reduced-motion` est respecté. Sur mobile, le bouton « Scorer les profils » est coupé par le bord de la fenêtre. Couleurs et tailles codées en dur (`bg-[hsl(40,3%,14%)]`, 20 lignes en `text-[Npx]`) : la démo ne suivra pas les jetons. | `LandingProductDemo.tsx:21`, `:69`, `:130-137`, `:141`. Capture `dark-mobile/landing.png` | Jouer une fois à l'entrée dans l'écran puis figer l'état final, ou bouton pause ; variante compacte sous `sm`. | toujours vrai (08, C.19) ; nouveau pour le mobile | Public |
| F-42 | P2 | / (landing, pied de page) | « Mentions légales » pointe vers `/privacy#mentions` : ni ancre ni section de mentions légales dans la page ; liens en `<a href>` (rechargement complet). | `SkalrLanding.tsx:621-622` ; `Privacy.tsx` (aucun `id`, `grep -n "id=" src/pages/Privacy.tsx` vide) | Section « Mentions légales » (obligation légale) avec ancre, `Link` du routeur. | toujours vrai (08, E.32) | Public |
| F-43 | P3 | / (landing) | Hiérarchie de conversion instable : hero = démo en plein et essai en contour, CTA final = l'inverse ; trois libellés pour aller à `/auth` (« Commencer », « Essai gratuit 14 jours, sans carte », « Découvrir la plateforme »). | `SkalrLanding.tsx:208-210`, `:344-350`, `:433-436`, `:603-609` | Un CTA principal constant (essai), la démo en secondaire. | toujours vrai (08, D.21-22) | Public |
| F-44 | P3 | /, /auth | Landing et auth notifient par l'ancien toaster Radix (`useToast`), le reste de l'app par sonner : deux apparences de notification. | `SkalrLanding.tsx:14`, `:102` ; `Auth.tsx:6`, `:52` ; `App.tsx:149-150` | sonner partout. | nouveau (le 9 sept. croyait sonner non monté : les deux sont montés) | Public |
| F-45 | P3 | /, /pricing | L'« accent » shadcn est un gris : pastilles d'eyebrow, carrés de numérotation, curseur de la bascule annuelle et puces de titres en `bg-accent` sont quasi invisibles, jamais l'indigo de la charte. | `SkalrLanding.tsx:313`, `:468`, `:533` ; `Pricing.tsx:329`, `:400`, `:581`, `:652` ; `src/index.css:32`, `:150` | Supprimer ces pastilles décoratives ; si un signal est voulu, l'accent indigo. | nouveau | Public |
| F-46 | P2 | /auth | Formulaire sans libellés (placeholders seuls) et erreurs d'authentification brutes du fournisseur, en anglais (« Invalid login credentials », « User already registered »). | `Auth.tsx:338-370` ; `Auth.tsx:274`, `:404` | `Label` visibles ; table de traduction des erreurs courantes, repli générique en français. | toujours vrai (08, E.27-28) | Public |
| F-47 | P2 | /auth | Trois rayons et deux dialectes dans un formulaire de 4 contrôles : champs `rounded-lg`, bouton principal natif carré en capitales, bouton Google du kit en pilule ; titre h2 (pas de h1) en capitales graisse 400 de 36 px ; « Chargement... ». | `Auth.tsx:324-326`, `:338-370`, `:374-382`, `:394-397`. Capture `dark-desktop/auth.png` | `Button` du kit pour les deux actions, même hauteur (40) ; titre h1 en casse de phrase, Instrument Sans 600 ; « Connexion… ». | toujours vrai (08, 2.4) | Public |
| F-48 | P3 | /auth | Page nue, sans rappel de la landing ni de l'app (aucune marque visible hormis le logo illisible, aucun visuel, aucun lien vers les tarifs ou la confidentialité) ; bouton Google sans état de chargement. Arrivé depuis un lien protégé (invitation de mission `/mission-invite/:token`), le visiteur voit « CONNEXION » sans un mot sur la raison : le bandeau d'invitation ne couvre que `?invitation=`. | `Auth.tsx:293-437`, `:394-407`, `:308-321` ; `App.tsx:160` ; `ProtectedRoute.tsx:24`. Capture `dark-desktop/invite-invalid.png` | Même coquille publique que `/pricing` (en-tête, pied avec liens légaux) ; `loading` sur le bouton Google. | toujours vrai | Public |
| F-49 | P2 | /pricing | Dégradés et violet hors charte : titre en texte dégradé violet-rose-bleu, pastille de remise en dégradé, barre du plan recommandé en dégradé (l'animation `gradientShift` qu'elle référence n'existe nulle part : barre figée), CTA recommandé en dégradé ; violet `--skalr-purple` codé en dur sur 9 éléments (bordure, badge, prix, coches, en-têtes du comparatif, bandeau d'essai). | `Pricing.tsx:204`, `:347`, `:409`, `:465-473` ; violet `:364-365`, `:440`, `:481`, `:505`, `:534`, `:540`, `:597`, `:624` ; `src/index.css:283-292` ; `grep -rn gradientShift src` (seul emploi `Pricing.tsx:470`) | Plan recommandé signalé par l'accent indigo (filet 1 px + badge), CTA recommandé en bouton plein monochrome, texte uni, pastille de remise neutre. | toujours vrai (07, §14) ; PR #245 non fusionnée | Public |
| F-50 | P2 | /pricing | Mouvement : toute la page apparaît par styles inline (opacité 0 → 1, translation 20 px) avec des délais cumulés jusqu'à ~1,1 s pour la dernière fonctionnalité ; survol par gestionnaires JS qui soulèvent la carte et ajoutent une ombre violette ; aucun `prefers-reduced-motion`. | `Pricing.tsx:134-137`, `:321-414`, `:447-462`, `:524-529`, `:557-578`, `:644-650` | Contenu visible d'emblée (ou fondu unique de 150-250 ms), survol par CSS (filet), réduction du mouvement respectée. | toujours vrai (07, §14) | Public |
| F-51 | P2 | /pricing | Dialecte brutal : cartes à bordures 2 px fusionnées, CTA carrés `h-12` en capitales, bascule mensuel/annuel faite main (curseur à peine visible en sombre), noms de plan, en-têtes et libellés du comparatif en 12 px capitales, h1 Outfit `font-black`. | `Pricing.tsx:199-207`, `:337`, `:385-404`, `:435-446`, `:492`, `:580-614`. Captures `dark-desktop/pricing.png`, `dark-mobile/pricing.png` | Cartes du kit, bascule = `Switch` ou segment du kit, CTA 40 px en casse de phrase, titres Instrument Sans (ou police de marque publique unique). | toujours vrai (07, anomalies C, E) | Public |
| F-52 | P3 | /pricing | États et renvois : erreur de chargement des tarifs sans « Réessayer » ; « Demandez à un administrateur de votre espace » sans nommer l'administrateur (les Paramètres le nomment) ; la FAQ renvoie à « Paramètres, Abonnement et crédits » en texte sans lien ; « Limites du plan » des Paramètres n'affiche pas la ligne « Recherches Base Konekt / mois » du comparatif. | `Pricing.tsx:420-423`, `:251-256`, `:27`, `:31`, `:35`, `:45` ; `BillingSettings.tsx:156-172` ; comparer `lib/settingsRoutes.ts:101-104` | `ErrorBox` avec réessai ; `managedBySentence` ; liens vers `/settings/org/billing` (connecté) ; même liste de limites des deux côtés. | toujours vrai (libellés FAQ mis à jour) | Public |
| F-53 | P1 | /client/:token | Étapes illisibles pour le client : le portail n'attend que 7 clés anglaises (`sourced` … `rejected`) alors que la même colonne `job_candidate_status.pipeline_stage` reçoit les clés de l'ATS (« CV envoyé », « Pressenti »…), celles du kanban de mission (`messaged`, `dismissed`) et, pour chaque étape d'entretien du process, l'identifiant technique de l'étape. Toute valeur inconnue s'affiche telle quelle avec 📋 : le client peut lire « 📋 dismissed » ou un UUID ; les KPI « À évaluer », « En process », « Embauchés » et le funnel ne les comptent pas. D'après le code, non vérifié sur données réelles. | `ClientPortalV2.tsx:76-90`, `:150-159`, `:1018-1020` ; `supabase/functions/client-portal-data/index.ts:107-111` ; `missions/MissionPipeline.tsx:325-329` (clé de colonne = `s.id`), `:346-348`, `:419-430` ; `hooks/useATSData.ts:50-60`, `:400`, `:416` | Une table d'étapes unique partagée (ATS, kanban, portails) avec libellé client ; repli lisible (« En cours ») au lieu de la valeur brute ; exclure ou nommer les candidats écartés. | nouveau (le 9 sept. relevait deux taxonomies ; l'identifiant d'étape et les clés du kanban sont nouveaux) | Portails (+ Pipeline) |
| F-54 | P1 | /portal/:token | Avancement figé ou faux pour le candidat : l'étape est copiée dans le jeton à la création du lien et aucun code du dépôt (front, fonctions, migrations, `MIGRATION_CLEAN.sql`) ne la met à jour, alors que la page annonce un suivi « en temps réel » et calcule « Étape actuelle depuis N jours » ; les étapes ATS absentes de la carte du portail (« Pressenti », « Perdu ») retombent sur l'ordre 0 : un candidat écarté lit « Candidature reçue » avec le badge « En cours » qui pulse. | `components/ats/CandidateDetailModal.tsx:276-289` (seule écriture) ; `CandidatePortal.tsx:42-51`, `:145-154`, `:165`, `:308-312` ; `hooks/useATSData.ts:50-60` | Lire l'étape vivante (jointure sur la candidature) ; carte complète des étapes avec un état « Processus terminé » neutre pour « Perdu » ; retirer « en temps réel » tant que ce n'est pas vrai. À confirmer en base (un déclencheur hors dépôt est possible). | nouveau | Portails |
| F-55 | P2 | /client/:token | Effets décoratifs sur tout le portail client : filets en dégradé violet-rose-bleu animés en boucle (en-tête, carte d'accueil, fiche), reflet balayant infini sur l'avatar de repli, l'icône et le bouton « C'est parti », prénoms en serif italique, emojis servant d'icônes (étapes 🆕 👋 🔍 🎯 📨 ✅ ❌, « ⭐ Top candidats », « 📈 Activité récente », 📊 🎯 🔒, 🔒 de l'état invalide) ; étincelles (signe IA) comme logo de repli ; `prefers-reduced-motion` non respecté par ces classes. | `ClientPortalV2.tsx:77-83`, `:182`, `:296`, `:303-305`, `:312`, `:360-363`, `:369`, `:377-389`, `:396`, `:509`, `:542`, `:989` ; `src/index.css:394-426` (`konekt-skalr-bg`, `konekt-shine`), `:450-457` (réduction du mouvement limitée à trois classes) | Filet neutre ou accent indigo fixe, pas de reflet, noms en Instrument Sans, icônes lucide, initiales de l'organisation en repli ; les 56 lignes en `text-[Npx]` du fichier passent aux paliers nommés. | toujours vrai (08, A.8, B.13, C.18) | Portails |
| F-56 | P2 | /client/:token | Tutoiement dans tout le portail client (« Bienvenue sur ton portail », « Donne ton feedback », « Essaie d'autres mots-clés ») alors que la scorecard du même écran vouvoie (« Vos impressions… ») ; la carte d'accueil promet « Ce lien est privé. Toi seul peux y accéder. » alors que tout détenteur du lien y accède. | `ClientPortalV2.tsx:366`, `:372`, `:379`, `:384`, `:389`, `:846`, `:1058` ; `PortalCandidateScoring.tsx:179` | Vouvoiement ; « Ce lien vous est personnel : ne le transférez pas. » | toujours vrai (08, A.6) ; nouveau pour la promesse | Portails |
| F-57 | P2 | /client/:token | Accessibilité : carte d'accueil en `div` plein écran sans `role="dialog"`, sans Échap ni piège de focus ; onglets sans `role="tab"`/`aria-selected` ; recherche et filtres en `input`/`select` natifs ; notes de 1 à 5 et recommandations en boutons sans `aria-pressed` ni groupe ; zone de commentaire en `focus:outline-none` sans remplacement. | `ClientPortalV2.tsx:353-404`, `:333-349`, `:805-830` ; `PortalCandidateScoring.tsx:129-144`, `:150-170`, `:180` | `Dialog`, `Tabs`, `Input`, `Select`, `RadioGroup`/`ToggleGroup`, `Textarea` du kit. | toujours vrai (08, E.26) ; le focus des champs de recherche est corrigé (`focus:ring-2`) | Portails |
| F-58 | P2 | /client/:token | Deux échelles pour un même score : fiche « Score 72/100 », scorecard envoyée sur 5 (« Score: 4.2/5 ») ; la scorecard est un accordéon carré en capitales posé dans une fiche arrondie ; « Évaluation envoyée » n'est pas conservé (le formulaire revient au rechargement). | `ClientPortalV2.tsx:1021-1025` ; `PortalCandidateScoring.tsx:36-47`, `:88`, `:109-119`, `:184-193` | Une échelle affichée partout ; état « évalué le … » relu du serveur ; accordéon du kit. | toujours vrai (08, A.5) | Portails |
| F-59 | P2 | /portal/:token | Dialecte brutal : blocs carrés, h1 en `font-black` capitales, eyebrows en capitales, bandeau inversé « AVANCEMENT DU PROCESSUS », repères carrés, boutons « EMAIL » / « APPELER » carrés en capitales, pied de page en capitales ; « Prochaine étape » en émeraude brute ; « Finalisé 🎉 » ; chargement sans texte. | `CandidatePortal.tsx:173`, `:184`, `:199`, `:210`, `:243`, `:261-264`, `:284-289`, `:309-316`, `:394`, `:416`, `:423`, `:432`, `:50`, `:122-127` | Même anatomie que le portail client une fois assaini (cartes 12 px, casse de phrase, pastilles d'étape), couleur réservée à l'étape courante (accent indigo). | toujours vrai (08, §4) | Portails |
| F-60 | P2 | /portal, /client, /r (états « lien invalide » et « introuvable ») | Trois écrans d'impasse différents pour le même cas : icône lucide + h1 20 px (candidat), emoji 🔒 + h1 Outfit 24 px (client), h1 en capitales sans icône + lien « Retour à l'accueil » (profil) ; aucun n'affiche la marque Konekt ni un moyen de contacter quelqu'un ; aucun ne distingue lien expiré et erreur réseau (le portail client affiche « invalide » sur toute exception). | `CandidatePortal.tsx:130-142` ; `ClientPortalV2.tsx:129-131`, `:177-189` ; `RecruiterPublicProfile.tsx:66-76`. Captures `dark-desktop/portal-invalid.png`, `client-invalid.png`, `recruiter-profile.png` | Un composant d'impasse public unique (logo, titre, phrase, action) ; distinguer « lien expiré » et « problème de connexion, réessayer ». | toujours vrai (08, E.34) | Portails |
| F-61 | P2 | /r/:slug | Profil public du recruteur : ombres décalées « brutales » (4 px et 3 px en `style` inline, uniques dans le dépôt), avatar en dégradé violet-rose, nom en capitales, statistique « Mid-round rate » en anglais, vidéo lancée en `autoPlay` et recadrée (`object-cover`), carré de chargement qui tourne sans être rond. | `RecruiterPublicProfile.tsx:62`, `:117`, `:123`, `:128`, `:160-161`, `:213`, `:270` | Carte du kit sans ombre décalée, avatar neutre (initiales), libellé français, vidéo sans lecture automatique en `object-contain`, `Loader2`. | toujours vrai (08, §6) | Portails |
| F-62 | P2 | /unsubscribe | L'état ne dit pas la vérité : un échec réseau (à la vérification comme à la confirmation) affiche « Lien invalide » en rouge, sans « Réessayer » ; la page n'a ni titre de document, ni logo, ni nom de l'expéditeur (« Vous ne recevrez plus d'emails de notre part » : de qui ?), ni lien de sortie ; succès en émeraude brute. | `Unsubscribe.tsx:16-24`, `:32-38`, `:42-80`, `:50`, `:59`, `:71-76`. Capture `dark-desktop/unsubscribe.png` | Distinguer « lien expiré » et « connexion impossible, réessayer » ; logo, nom de l'organisation expéditrice, `SEOHead` ; jeton `success` ; icône neutre pour un lien expiré. | toujours vrai (08, §9) | Public |
| F-63 | P3 | /privacy, /privacy-extension | Deux pages légales, deux gabarits : titre en `font-black` capitales (/privacy) contre titre mixte « Privacy — Extension Chrome » avec icône (/privacy-extension) ; sections en `text-lg` capitales ; liens soulignés blancs d'un côté, bleus `text-info` de l'autre ; puces ❌ ; dates « mars 2026 » contre « 2026-04-23 » ; deux contacts (`privacy@konekt.io`, adresse personnelle `l.garilhe@konekt.fr`) ; anglicismes « Privacy », « Privacy général → ». Les noms de sous-traitants y sont légitimes (exception RGPD). | `Privacy.tsx:7`, `:26-33`, `:39`, `:56` ; `PrivacyExtension.tsx:13`, `:32-39`, `:48`, `:54`, `:96-100`, `:137`, `:145` | Un gabarit légal commun (en-tête public, titre en casse de phrase, table des matières), une adresse de contact unique, dates au même format, « Confidentialité de l'extension Chrome ». | toujours vrai (08, §7-8) ; liens vers les Paramètres corrigés (`/settings/account/connections#extension`) | Public |
| F-64 | P3 | /unsubscribe, /privacy, /privacy-extension, /r/:slug | Pages publiques absentes de `PUBLIC_ROUTES` : si une session locale expire pendant la lecture, la fenêtre « Session expirée » de l'app peut s'ouvrir sur une page qui ne demande aucune connexion. | `App.tsx:53`, `:95-109` | Ajouter ces routes à la liste (ou inverser la logique : liste des routes protégées). | toujours vrai (08, E.31) | Public |
| F-65 | P3 | Bandeau d'essai (toutes pages de l'app) | Bandeau d'essai calme et honnête (`role="status"`, teinte avertissement), mais sans fermeture : « Essai terminé » reste affiché sur chaque page pour une organisation gratuite ; l'administrateur est renvoyé vers la page publique `/pricing` (autre dialecte, logo illisible) plutôt que vers « Abonnement et crédits » ; le membre lit « Demandez à un administrateur » sans son nom. | `billing/TrialBanner.tsx:26-44` ; comparer `lib/settingsRoutes.ts:101-104` | Fermeture mémorisée pour l'état « Essai terminé » ; lien vers `/settings/org/billing` ; `managedBySentence`. | nouveau | À remettre à la session Paramètres |
| F-66 | P3 | /settings | Chargements et confirmations sans forme commune : `BrutalLoader` (Journal), `Loader2` seul (extension), `Loader2` + « Chargement... » (modèles), texte seul (Base Konekt), squelette qui pulse (coquille) ; bouton de confirmation destructive stylé de trois façons (`bg-destructive` seul, avec survol, avec couleur de texte). | `AgentActionsSettings.tsx:404` ; `ExtensionTokens.tsx:225` ; `MessageTemplatesSettings.tsx:266-272` ; `BaseKonektCard.tsx:71-76` ; `Settings.tsx:87`, `:189` ; `OrgLogoEditor.tsx:221`, `PedigreePresetsSettings.tsx:180`, `AgentConnectorsSettings.tsx:398` contre `EmailSignatures.tsx:133` et `MyLinkedInAccount.tsx:413` | Squelette pour les blocs de contenu, `Loader2` dans le bouton pour les actions ; `AlertDialogAction` avec une variante `destructive` dans le kit. | toujours vrai (07, §15 et anomalie D) | À remettre à la session Paramètres |
<!-- FIN TABLEAU -->

## Corrigé depuis le 9 septembre (vérifié dans le code)
- **Paramètres** : 13 onglets en 4 groupes remplacés par 7 rubriques en 2 portes, avec un en-tête identique (titre + phrase), une liste mobile dédiée, la note « gérés par … » pour les membres et la redirection des anciennes adresses `?tab=` (`src/pages/Settings.tsx`, `shell/sections.tsx`, `lib/settingsRoutes.ts:29-76`).
- **Onglets retirés avec leurs défauts** (commit `90423c37`) : Agence (5 pseudo-interrupteurs sans rôle, « Owners », « bounties », « transcripts »), Connecteurs (désactivation sans confirmation, badge « ACTIF »), Marketplace dans les Paramètres (carte faite main), WhatsApp (style inline, pas de déconnexion), modèle IA par défaut (clé locale purgée, `main.tsx:115-121`).
- **Confirmations** : suppression du logo (`OrgLogoEditor.tsx:199-226`) et dissociation d'un compte LinkedIn introuvable (`MyLinkedInAccount.tsx:465-490`) passent par `AlertDialog`.
- **Erreurs de lecture explicites** : abonnement (« annonçait Gratuit à un client payant »), plages LinkedIn, contexte IA, modèles, signatures, statistiques d'enrichissement, compte LinkedIn (voir « Ce qui sert de modèle »).
- **Plafond d'actions** : une seule borne (1 à 500) pour l'équipe et le libre-service (`hooks/useMemberQuotas.ts:38-39`), au lieu de 200 contre 500.
- **Aperçu de signature** assaini (`EmailSignatures.tsx:22`, `:31`).
- **Libellés** : « Importez votre logo » au lieu de « Uploadez un logo custom » ; forfait exprimé en « Emails de contact / mois (un mobile en vaut 10) » dans les Paramètres et le comparatif des tarifs ; la FAQ des tarifs renvoie à « Paramètres, Abonnement et crédits ».
- **Liens** : la confidentialité de l'extension et l'état « type manquant » de la marketplace pointent vers les nouvelles adresses (`PrivacyExtension.tsx:87`, `:138` ; `Marketplace.tsx:78`).
- **Marketplace** : messages d'erreur serveur traduits (`hooks/useMarketplace.ts:136-157`).
- **Portail client** : les champs de recherche et de filtre ont un anneau de focus (`ClientPortalV2.tsx:809`, `:816`, `:830`).
- **Toasts** : les deux systèmes sont montés (`App.tsx:149-150`) ; le 9 septembre croyait sonner non monté. Il reste deux apparences (F-44).

## PR #245 (non fusionnée) : ce qu'elle fait sur Pricing, ce qui reste
- **Ce qu'elle change** (commit `a20221be`, 3 septembre) : titre en texte uni au lieu de `skalr-gradient-text` ; pastille « -20% » en `bg-foreground text-background` au lieu du dégradé ; barre du plan « Pro » en violet uni au lieu du dégradé animé ; CTA « Pro » en violet uni au lieu du dégradé.
- **Pourquoi elle ne s'applique plus telle quelle** : elle vise une version antérieure du fichier (remise « -20% » et plan « Pro » codés en dur). `Pricing.tsx` a depuis été réécrit (plans lus en base, remise calculée `:126-132`, plan recommandé `cabinet`/`solo` `:109`, bandeau d'essai `:363-369`, CTA par état `:195-284`) et garde les quatre dégradés (`:204`, `:347`, `:409`, `:465-473`).
- **Ce qui resterait à faire même appliquée** : le violet `--skalr-purple` codé en dur sur 9 éléments, que la PR étend à la barre et au CTA (l'étalon veut un accent indigo rationné et un bouton principal monochrome) ; les apparitions en style inline et le survol par gestionnaires JS (`:321-462`, `:524-578`) ; le dialecte brutal (bordures 2 px fusionnées, CTA carrés `h-12` en capitales, noms de plan en capitales) ; la bascule faite main ; l'eyebrow et les puces en `bg-accent` gris ; le logo bleu marine sur fond sombre ; l'erreur de chargement sans « Réessayer ». Détail en F-34, F-45, F-49 à F-52.
- La partie `EmptyMissionState.tsx` de la même PR relève du lot missions (audit B).

## Synthèse chiffrée

| | P1 | P2 | P3 | Total |
|---|---|---|---|---|
| À remettre à la session Paramètres (dont bandeau d'essai) | 1 | 14 | 11 | 26 |
| Marketplace | 0 | 3 | 2 | 5 |
| Public (landing, auth, pricing, légal, désinscription) | 2 | 14 | 7 | 23 |
| Portails (candidat, client, profil recruteur) | 2 | 7 | 0 | 9 |
| Transverse (jetons, primitives) | 0 | 1 | 2 | 3 |
| **Total** | **5** | **39** | **22** | **66** |

54 constats étaient déjà relevés le 9 septembre et restent vrais (certains aggravés ou précisés) ; 12 sont nouveaux. Les cinq P1 : F-10 (procédure d'installation de l'extension avec le chemin d'un poste de développeur), F-33 (« Réserver une démo » ouvre un agenda générique), F-34 (logo bleu marine illisible sur /auth et /pricing), F-53 (étapes brutes, clés anglaises ou UUID, affichées au client), F-54 (avancement du candidat figé à la création du lien, candidat écarté affiché « Candidature reçue — En cours »).

## Par écran : 3 à 5 changements vers la cible

### /settings (à remettre à la session Paramètres)
1. Une seule anatomie de carte : titre `text-sm font-semibold` en casse de phrase, action à droite, pas de capitales ni de `text-lg` (F-01, F-02, F-05).
2. Rubrique active au même traitement que la barre latérale de l'app ; titres en Instrument Sans (F-03, F-04).
3. Chaque lecture a trois états honnêtes (chargement, erreur avec « Réessayer », vide) : combler Journal, ICP, e-mail, politiques ; aligner libellé et couleur du statut LinkedIn (F-06, F-07, F-25).
4. Une règle d'enregistrement écrite et un seul verbe ; confirmation pour le type d'organisation et le rôle (F-09, F-21).
5. Couleur rendue aux signaux : consommation neutre, badges d'état en pastille, plus d'ambre ni d'émeraude bruts ; puis finitions (vouvoiement, emojis, surfaces carrées, libellés tronqués, mobile) (F-11 à F-19).

### /marketplace
1. Une seule anatomie de carte (kit, rayon 12, casse de phrase) pour les cinq listes du module, y compris l'état « type manquant » (F-26).
2. Un bouton plein par vue ; les actions de carte en contour, compétences en texte discret, dialogue « Postuler » en `DialogFooter` + `Button` (F-27).
3. Contrôles du kit (`Input`, `Select`, `Tabs`, `Label`) : focus visible, onglets annoncés, libellés reliés (F-28).
4. Primitives d'état et de page (`PageHeader`, `EmptyState`, squelette, `ErrorState` rangé dans `ui/`) (F-29, F-30, F-32).
5. Contenu : adresse d'équipe, tuile d'icône neutre, trait d'union (F-31).

### / (landing)
1. Supprimer le forçage du thème clair et aligner la landing sur le thème et les jetons de l'app (F-35).
2. Remplacer `BrutalButton`, cartes `border-2` et capitales par le kit ; un seul CTA principal constant (F-36, F-43).
3. Retirer dégradés ciel, jaune « Match IA », filet violet-rose et ombres sur l'inerte ; une seule police de titrage publique (F-37, F-38, F-45).
4. Réparer le parcours : vraie prise de rendez-vous, section « Mentions légales », preuves sourcées ou retirées (F-33, F-39, F-42).
5. Surcouches du kit (dialogue, tiroir, accordéon) et démo jouée une fois puis figée, compacte sur mobile (F-40, F-41).

### /auth
1. Logo lisible (variante selon le thème) (F-34).
2. Libellés visibles et erreurs traduites (F-46).
3. `Button` du kit pour les deux actions, titre h1 en casse de phrase (F-47).
4. Même coquille publique que la page Tarifs (en-tête, liens légaux), toasts sonner (F-44, F-48).

### /pricing (et PR #245)
1. Retirer les quatre dégradés sur le fichier actuel (la PR #245 les visait sur une version antérieure du fichier : libellés « -20% » et « Pro » codés en dur, avant les plans lus en base ; elle ne s'applique plus telle quelle) (F-49).
2. Remplacer le violet `--skalr-purple` codé en dur (9 éléments, que la PR conservait et étendait à la barre et au CTA) par l'accent indigo rationné au seul plan recommandé (F-49).
3. Supprimer les animations inline et les gestionnaires de survol JS, que la PR ne touchait pas (F-50).
4. Passer cartes, bascule, CTA et comparatif au kit, casse de phrase (F-51).
5. Logo, états (réessai), renvois cliquables, nom de l'administrateur (F-34, F-52).

### Portails (candidat, client) et profil public
1. Dire la vérité sur l'avancement : une table d'étapes unique, étape vivante côté candidat, repli lisible, jamais de clé technique ni d'UUID à l'écran (F-53, F-54).
2. Retirer dégradés animés, reflets, serif italique et emojis du portail client ; icônes lucide (F-55).
3. Vouvoiement et promesses exactes (lien personnel, pas « toi seul ») (F-56).
4. Primitives du kit (dialogue, onglets, champs, groupe de notes) et une seule échelle de score (F-57, F-58).
5. Une seule anatomie pour les trois pages externes (candidat, client, recruteur) et un seul écran d'impasse avec la marque (F-59, F-60, F-61).

### /privacy, /privacy-extension, /unsubscribe
1. Désinscription honnête : distinguer lien expiré et erreur réseau, proposer « Réessayer », nommer l'expéditeur (F-62).
2. Un gabarit légal commun aux deux pages, avec la section « Mentions légales » que la landing promet (F-42, F-63).
3. Une adresse de contact d'équipe unique, dates au même format (F-63).
4. Toutes les pages publiques déclarées publiques, marque visible (logo lisible) sur chacune (F-34, F-64).

## Ce qui sert de modèle
- **Coquille des Paramètres** (`src/pages/Settings.tsx`) : en-tête de rubrique identique partout (titre + une phrase), liste mobile à cibles de 48 px avec chevrons et retour de focus (`:113-120`, `:122-139`), focus porté au titre après navigation (`:166-172`), note « gérés par … » pour les membres (`:188-196`).
- **Erreurs honnêtes** : `ErrorBox` réutilisé (`BillingSettings.tsx:180-194`, `LinkedInSafetySettings.tsx:149-156`, `AiContextSettings.tsx:69-74`), avec la phrase « Vos réglages enregistrés ne sont pas affectés ».
- **États LinkedIn** (`MyLinkedInAccount.tsx:328-342`, `:583-598`) : chargement et erreur distincts, jamais de faux « non relié ».
- **Indicateur d'enregistrement** du contexte IA (`AiContextSettings.tsx:331-359`) : « Modifications non enregistrées / À jour », « Annuler » seulement quand il y a quelque chose à annuler.
- **Divulgation progressive** : connecteurs de l'assistant sous « Options avancées pour développeurs » (`AgentConnectorsSettings.tsx:206-219`), carte de l'extension masquée tant qu'aucun jeton n'existe (`ExtensionTokens.tsx:65-70`, `:154`).
- Kit majoritairement employé : 94 `<Button>` contre 14 `<button>` natifs dans le périmètre Paramètres.
- **Page Tarifs, logique des CTA** (`Pricing.tsx:195-284`) : chaque état du visiteur a son bouton et son libellé (chargement, essai, création d'espace, plan actuel, administrateur requis, portail de gestion, paiement), jamais un bouton qui mène à une impasse. Même rigueur dans `BaseKonektCard.tsx:100-170` (Suspendue / Activée / Non activée, coût annoncé avant usage).
- **Erreurs lisibles de la marketplace** (`hooks/useMarketplace.ts:136-157`) : les messages techniques sont remplacés par une phrase française, le détail part en console ; chaque liste du module a son `ErrorBox` avec « Réessayer ».
- **Bandeau d'essai** (`billing/TrialBanner.tsx`) : une ligne, `role="status"`, ton factuel, action adaptée au rôle.
- **Démo produit de la landing** (`LandingProductDemo.tsx`) : c'est la surface publique la plus proche de la cible (neutres sombres, bouton principal blanc, filets fins, un seul signal chiffré) et elle respecte `prefers-reduced-motion` (`:69`). À garder comme référence visuelle, à brancher sur les jetons.
- **Bandeau d'invitation de /auth** (`Auth.tsx:308-321`) : l'écran dit qui invite et quoi faire selon que l'on a un compte ou non.
