# Audit design et UX, lot B : missions et onboarding

> Revue du 25 septembre 2026, en lecture seule, sur `main` au commit `5882084`, avant les lots 1 et 2 du chantier design. Les captures citées viennent du banc visuel local (`docs/design/05-banc-visuel.md`) et ne sont pas versionnées. Les textes entre guillemets sont cités tels qu'ils apparaissent dans l'application. Statut de chaque constat : `docs/design/02-constats.md`.

Date : 2026-09-25. Branche `claude/design-ux-audit-g8ovch` (HEAD `5882084`). Audit en lecture seule : aucun fichier du dépôt modifié.

Étalon : registre « calm premium » (Linear, Qonto). Neutres chauds en teinte 40, un seul accent indigo désaturé (`--k-accent`, `src/index.css:107` et `:187`) réservé au focus, à la sélection, à la priorité et à l'action. Pas de dégradé violet-rose, de glow, de shimmer, de particules ni de fond animé. Peu de familles typographiques, des micro-interactions de 150 à 250 ms, des états toujours explicites et une aide contextuelle utile.

## 0. Périmètre, méthode, priorités

**Fichiers lus en entier** (par tranches au-delà de 1000 lignes) :
- pages : `src/pages/Outreach.tsx`, `MissionWorkspace.tsx`, `Onboarding.tsx`, `Qualification.tsx`, `AcceptMissionInvite.tsx` ;
- missions : tout `src/components/missions/**`, soit `v2/` (MissionWorkspaceV2, PhaseStepper, Pill, MissionOverviewV2, MissionBriefV2, MissionProcessV2, MissionConfigV2, CreateMissionV2), `process/shared.tsx`, EmptyMissionState, EmptyLinkedInAccountState, MissionQuotaNotice, MissionSourcing, SourcingReadinessPanel, MissionOutreach, MissionPipeline, MissionInsights, MissionHuntMode, MissionClientPortal, FilterReviewModal et VoiceDictation ;
- projets : tout `src/components/outreach/projects/**` (ProjectsListV2, ProjectCandidatesTableEnhanced, ProjectFunnel, AddToProjectButton, index) ;
- onboarding : tout `src/components/onboarding/**`.

Pour vérifier les constats, j'ai aussi lu les jetons (`src/index.css`, `tailwind.config.ts`), les primitives `ui/button.tsx`, `ui/dialog.tsx` et `ui/brutal-loader.tsx`, ainsi que les hooks appelés (`useSourcingProjects`, `useMissionProcess`, `useMissionReadiness`, `useMissionInvitations`, `missionUtils`). Côté serveur : les fonctions `accept-mission-invitation` et `enrich-company`, lues sur des extraits ciblés.

**Code mort détecté**, parcouru par motifs et non audité ligne à ligne. Aucun import dans `src/` :
- `src/components/missions/FilterWizard.tsx` (762 lignes) ;
- `src/components/missions/PedigreePresetSelector.tsx` (332 lignes) ;
- `src/components/outreach/projects/CreateProjectModal.tsx` (1133 lignes, lu jusqu'à la ligne 770 puis parcouru par motifs ; exporté par `index.ts`, jamais importé).

**Priorités**
- **P1** : information fausse affichée, parcours cassé ou action sans effet, règle de marque violée, accessibilité ou contraste bloquant, effet décoratif sur l'action principale.
- **P2** : incohérence visible, état manquant (chargement, vide, erreur, enregistrement), accessibilité non bloquante, écart au registre.
- **P3** : finition.

**PR #245** (ouverte, non fusionnée, `mergeable_state: dirty`) : rien n'est appliqué sur la branche actuelle.
- `EmptyMissionState.tsx` garde tous les effets que la PR retirait (détail en B-09).
- `src/pages/Pricing.tsx` garde le texte en dégradé (l. 347), le badge « -20 % » en dégradé (l. 409), la barre du plan Pro avec `gradientShift` (l. 467-470) et le CTA en dégradé (l. 204).
- Même fusionnée, la version de la PR laisserait dans EmptyMissionState : les chiffres non sourcés (« 200M+ », « 45s », « 3x »), un bouton imbriqué dans un bouton, le lien « page carrières » sans action, les titres `font-black uppercase` et les angles vifs.

**Mesures sur le code vivant du périmètre** (43 fichiers `.tsx`, code mort exclu)

| Mesure | Valeur |
|---|---|
| Tailles `text-[Npx]` arbitraires | 252 (75 × 10px, 63 × 11px, 44 × 12px, 21 × 13px, 13 × 11.5px…) alors que `tailwind.config.ts:146` les bannit |
| `<button>` / `<motion.button>` bruts contre `<Button>` | 112 contre 28 |
| `<input>`, `<textarea>`, `<select>` natifs | 38 |
| `uppercase tracking-wide(r)` | 104 |
| Lignes d'UI contenant un emoji ou un glyphe-icône | 72, dans 16 fichiers |
| Classes `konekt-skalr-bg`, `-soft`, `-text`, `konekt-shine`, `konekt-shimmer-text` | 28 usages, dans 6 fichiers |
| Usages des jetons `--k-*` (accent indigo) | 0 dans le périmètre (seulement dans `src/components/outreach/search/*`) |
| Fichiers avec `font-editorial` (serif italique) | 10 |

---

## 1. Tableau des constats

Les chemins sont relatifs à la racine du dépôt. « l. » renvoie à une ligne.

| ID | Prio | Écran | Constat | Preuve | Correctif proposé |
|---|---|---|---|---|---|
| B-01 | P1 | Acceptation d'invitation | Après un succès, la page bascule sur « Invitation invalide » avec un toast d'erreur. L'effet dépend de `accept`, recréé à chaque rendu. Le rendu qui suit le succès relance `accept(token)`, et le serveur ne trouve plus d'invitation `pending` (404 « Invitation introuvable ou déjà utilisée »). | `src/pages/AcceptMissionInvite.tsx:14-27` ; `src/hooks/useMissionInvitations.ts:134-159` (fonction non mémorisée) ; `supabase/functions/accept-mission-invitation/index.ts:57-69` | Appeler une seule fois : garde `useRef` ou mutation React Query déclenchée au montage, `accept` en `useCallback`. Distinguer les erreurs (expirée, autre adresse, réseau avec « Réessayer »). |
| B-02 | P1 | Vue d'ensemble | La carte « Process recruteur » affiche un process inventé (« Screening 30 min, Entretien tech 60 min, Culture fit 45 min, Offre sous 48h »), même quand l'utilisateur a configuré le sien. Elle lit `job_details.process_steps`, que rien n'écrit : les étapes vivent dans la table `mission_process_steps`. La complétude « process » de `computeReadiness` est donc toujours fausse. | `src/components/missions/v2/MissionOverviewV2.tsx:398-431` ; `src/hooks/useMissionProcess.ts:69, 151` ; `src/hooks/useMissionReadiness.ts:40, 60-67` ; aucun écrivain de `process_steps` dans `src/` ni `supabase/functions/` | Lire `useMissionProcess(project.id)`. Sans étape : état vide « Aucune étape définie » avec le bouton « Configurer le process ». Corriger `computeReadiness`. |
| B-03 | P1 | Vue d'ensemble | Le funnel affiche toujours 0 pour « Répondu », « Entretien » et « Offre » : `stats_replied`, `stats_qualified` et `stats_hired` n'existent ni dans le type ni dans la table. | `MissionOverviewV2.tsx:210-216` ; `src/hooks/useSourcingProjects.ts:27-31` ; `src/integrations/supabase/types.ts:5751+` (sourcing_projects : dismissed, messaged, scored, shortlisted, total_found) | Brancher sur `useProjectStats` comme le pipeline, avec les mêmes libellés que les colonnes du pipeline. |
| B-04 | P1 | Coquille mission | Le stepper coche en vert toute phase située avant la phase affichée, sans lien avec la complétude. Ouvrir « Pipeline » sur une mission sans brief affiche Cadrage et Sourcing « terminés ». | `src/components/missions/v2/PhaseStepper.tsx:48-52` (`phaseId < active → 'done'`), `:88-102` | Dériver l'état de `useMissionReadiness` (terminé, en cours, à faire, verrouillé) et cocher seulement si la phase est complète. |
| B-05 | P1 | Liste des missions | Le KPI « Réponses » affiche le nombre de shortlistés. Sur la même carte, ce même nombre s'appelle « qualifiés ». Le KPI global « Réponses reçues » additionne les shortlistés. Le commentaire parle de « réponses non traitées », mais le libellé dit « candidats sourcés non contactés ». | `src/components/outreach/projects/ProjectsListV2.tsx:250, 145, 466-473, 588, 93-100` | Une source de KPI unique, avec des libellés identiques aux colonnes du pipeline. Pas de « Réponses » tant qu'aucun compteur de réponses n'existe. |
| B-06 | P1 | Résultats de recherche vers mission | « Shortlister » ajoute le candidat avec le statut `untreated` mais affiche « X shortlisté pour… ». Or « Shortlisté » est aussi un statut et une colonne du pipeline : le même mot désigne deux choses. | `src/components/outreach/projects/AddToProjectButton.tsx:112, 124, 159, 179` ; `src/components/missions/MissionPipeline.tsx:72` ; `ProjectCandidatesTableEnhanced.tsx:91, 397-402` | Renommer l'action en « Ajouter à la mission » et réserver « Shortlister » au changement de statut. |
| B-07 | P1 | Onboarding et Brief | Noms de fournisseurs visibles, contraire à la règle de marque de CLAUDE.md. **Apollo** : `enrich-company` renvoie `source: 'Apollo'` et le badge de source des postes ouverts l'affiche tel quel. **Deepgram** : toast « Erreur de connexion Deepgram ». | `supabase/functions/enrich-company/index.ts:1335, 1599-1605` → `src/components/onboarding/SceneOrganization.tsx:896-900, 948` ; `src/components/missions/VoiceDictation.tsx:121` | Mapper les sources vers des libellés neutres (« Base Konekt », « LinkedIn », « Site carrière »), côté serveur ou dans `SourceBadge`. Message : « La dictée est indisponible. Réessayer ». |
| B-08 | P1 | Brief, revue des filtres | Modale maison sans `role="dialog"`, sans piège de focus et sans fermeture par Échap. Le fond `bg-foreground/60` produit un **voile blanc à 60 %** dans le thème sombre par défaut. Le registre est brutaliste (angles vifs, `border-2`, `font-black uppercase`), au moment de la validation IA. `if (!open) return null` empêche l'animation de sortie. | `src/components/missions/FilterReviewModal.tsx:334-525`, `:347`, `:363-419`, `:496-518` ; `src/index.css:15` (`--foreground: 0 0% 98%`) | Réécrire sur `Dialog` shadcn (ou `Sheet` en mobile), overlay `bg-black/60`, chips via un composant `ChipInput` partagé, casse phrase. |
| B-09 | P1 | État vide de /missions | La PR #245 n'est pas appliquée (voir 0) : fond neuronal animé, 20 particules, anneau orbital, texte machine à écrire, badge « Recommandé » pulsant, coins animés, ShimmerButton, flèche en boucle, logos HubSpot et Salesforce (aucune intégration de ce type dans le code). S'y ajoutent : un **bouton imbriqué dans un bouton** (ShimmerButton rend un `<button>`), `focus-visible:outline-none` sans anneau sur les deux cartes, et des particules re-tirées au hasard à chaque survol (`Math.random` dans le rendu, re-rendu par `setHoveredCard`). | `src/components/missions/EmptyMissionState.tsx:13-73, 76-113, 131-174, 177-205, 216-218, 252-308, 363, 371, 407-422, 473-486, 508, 315` ; `src/components/magicui/shimmer-button.tsx:32` | Voir l'écran 2 en section 2 : cartes `rounded-xl` entièrement cliquables sans bouton enfant, anneau de focus, aucun décor, pas de chiffres ni de logos. |
| B-10 | P1 | Liste, création, Brief, Process, lancement de l'onboarding | Les CTA principaux utilisent un dégradé violet, rose et bleu animé en continu (8 s), avec un reflet infini : « Nouvelle mission », « Analyser avec l'IA », « Réoptimiser avec l'IA », « Suggestion IA », « Créer la mission », « Pré-remplir ». Dans la création, on trouve aussi une barre dégradée, un badge dégradé et du texte dégradé. | `src/index.css:394-397, 411-423` ; `ProjectsListV2.tsx:575` ; `v2/MissionBriefV2.tsx:211` ; `v2/MissionProcessV2.tsx:199, 483` ; `v2/CreateMissionV2.tsx:576, 591, 671, 681, 705, 723-725, 755, 764, 942, 983` ; `onboarding/SceneLaunch.tsx:147` | `Button variant="primary"` (fond plein). Si l'on veut distinguer l'action IA : fond `--k-accent`, sans animation. Supprimer `konekt-skalr-*` et `konekt-shine`. |
| B-11 | P1 | Pipeline, vue tableau | Scores illisibles : `text-warning-foreground` (gris à 10 %, soit du quasi noir) sur une teinte à 10 % donne environ 1,2:1 en thème sombre pour les scores de 40 à 69. `text-success-foreground` (blanc) sur vert pâle rend les scores ≥ 70 illisibles en thème clair. Même défaut pour « Contacté », la barre de sélection et le menu de ligne. | `src/components/outreach/projects/ProjectCandidatesTableEnhanced.tsx:493-494, 88, 390, 547, 565, 576` ; `src/index.css:68, 71, 206, 209` | `text-success`, `text-warning` ou `text-destructive` sur les teintes ; `*-foreground` seulement sur fond plein. Composant `ScoreBadge` unique. |
| B-12 | P1 | Pipeline, vue tableau | L'entrée « Envoyer un message » du menu de ligne ne fait rien : `onOpenMessage` n'est pas fourni par le seul appelant. Les lignes n'ouvrent pas non plus la fiche candidat, que seul le kanban ouvre. | `ProjectCandidatesTableEnhanced.tsx:545-550` ; `MissionPipeline.tsx:581-585` | Brancher l'action (conversation ou inscription en séquence) ou la retirer. Rendre la ligne cliquable vers `CandidateDetailModal`. |
| B-13 | P1 | Transverse | Tutoiement et vouvoiement mélangés **dans un même écran**. /missions : « Tes missions », « demandent ton attention », « Essaye » contre « Lancez votre première mission » dans l'état vide. Configuration : « Pour qui recrutes-tu ? », « ton équipe » contre « Proposez cette mission… vous acceptez » (mode chasse). Portail client : « Entrez le nom » contre « Génère un lien ». Création : « Votre saisie… » contre « vérifie et crée ». Brief, Process, Config, Vue d'ensemble, création et Qualification tutoient ; l'onboarding, le sourcing, l'outreach, le pipeline, les insights et les toasts des hooks vouvoient. | `ProjectsListV2.tsx:548, 559, 614, 704` ; `EmptyMissionState.tsx:337, 345` ; `v2/MissionConfigV2.tsx:151, 387, 406` ; `MissionHuntMode.tsx:164` ; `MissionClientPortal.tsx:54, 176` ; `v2/CreateMissionV2.tsx:301, 346, 602-604` ; `pages/Qualification.tsx:306, 323, 350` | Trancher : le vouvoiement, cohérent avec l'onboarding, le B2B et les toasts. Puis une passe de relecture des chaînes. |
| B-14 | P1 | Process | Une étape d'entretien se supprime d'un clic, sans confirmation ni décompte des candidats positionnés dessus, alors que le remplacement par template est confirmé avec ce décompte. L'annulation d'une invitation externe n'est pas confirmée non plus. | `src/components/missions/process/shared.tsx:212`, `:650-656` ; `src/hooks/useMissionProcess.ts:191-203` ; comparer `v2/MissionProcessV2.tsx:150-178` | `AlertDialog` avec `countCandidatesOnSteps()`, ou suppression suivie d'un toast « Annuler ». |
| B-15 | P2 | Coquille mission | La racine `h-screen` est placée sous l'en-tête `h-12` d'AppLayout : la page dépasse de 48 px, avec deux défilements (document et zone interne). En mobile, `100vh` ignore la barre du navigateur. | `v2/MissionWorkspaceV2.tsx:149` ; `src/components/AppHeader.tsx:53` ; `src/components/AppLayout.tsx:42-60` | `h-full min-h-0` (le `main` est déjà `flex-1 min-h-0`) ou `h-[calc(100dvh-3rem)]`. |
| B-16 | P2 | Coquille mission | Onglets et phases verrouillés muets : `disabled` bloque le clic, donc les toasts prévus (« Complétez les étapes précédentes… ») ne partent jamais, et aucune infobulle n'explique le verrou. Le verrou est un emoji 🔒. | `MissionWorkspaceV2.tsx:119-124, 194-209` ; `PhaseStepper.tsx:66-80` ; toast inatteignable `MissionWorkspaceV2.tsx:133-135` | `aria-disabled` et `Tooltip` affichant `blockerMessage` (déjà fourni par `useMissionReadiness.ts:103-118`), icône `Lock` de lucide. |
| B-17 | P2 | Coquille mission | Le survol des phases ne s'affiche jamais : le style inline `background: 'transparent'` écrase `hover:bg-accent/50`. Couleurs en styles inline, tailles `text-[10px]` et `text-[12.5px]`, transitions de 300 à 500 ms. `desc` (« Brief & process »…) n'est jamais affiché. Un `<p>` est placé dans un `<button>`. | `PhaseStepper.tsx:75-83, 86-100, 104-111, 114-119` ; `src/lib/missionViews.ts:32, 43, 52` | Classes Tailwind pilotées par `data-state`, transitions de 150 à 200 ms, `desc` en sous-titre ou en infobulle, `<span>` au lieu du `<p>`. |
| B-18 | P2 | Coquille mission, mobile | Stepper et sous-onglets en `overflow-x-auto scrollbar-hide`. Estimation : environ 450 px pour les trois phases, pour 375 px disponibles, sans indice qu'il faut faire défiler. Les sous-onglets n'ont ni `role="tablist"`/`tab` ni `aria-selected`, et utilisent `text-[13px]`. | `PhaseStepper.tsx:62` ; `MissionWorkspaceV2.tsx:188-211` | Sous `sm` : stepper compact (phase active avec « 1/3 ») ou `Select`. Sous-onglets en `Tabs` shadcn, avec un fondu en bord. |
| B-19 | P2 | Vues mission | En-têtes de vue incohérents. Deux `h1` sur la Vue d'ensemble (fil d'Ariane de 14 px et titre de 28-32 px). Brief, Process et Config ont un sur-titre « Étape 1 · Cadrage » et un `h2` de 24 px. Sourcing, Outreach, Pipeline et Insights n'ont aucun en-tête. | `MissionWorkspaceV2.tsx:160` ; `v2/MissionOverviewV2.tsx:236` ; `v2/MissionBriefV2.tsx:183-189` ; `v2/MissionProcessV2.tsx:184-192` ; `v2/MissionConfigV2.tsx:73-79` | Composant `ViewHeader` (titre `h2`, description, actions) pour les 8 vues, et un seul `h1` dans l'en-tête. |
| B-20 | P2 | Vue d'ensemble | Plus aucune « prochaine action » : `computeNextStep` est conservé mais n'est plus rendu. Les seules actions sont deux boutons texte de 10 px (« Modifier → », « Configurer → »). | `MissionOverviewV2.tsx:88-160, 201-203, 274-279, 289-295, 342-348` | Une ligne « Prochaine étape », sobre (phrase et bouton secondaire), sans carte héro. Cibles d'au moins 32 px, et 44 px en mobile. |
| B-21 | P2 | Vue d'ensemble, Process | La pastille `Pill` « ai » utilise un dégradé violet-rose codé en dur (« Brief structuré par IA »). Le point pulsant infini s'affiche aussi sur des statuts non temps réel (« Outreach actif », « Process configuré »). | `src/components/missions/v2/Pill.tsx:37-41, 60-65` ; `MissionOverviewV2.tsx:233, 363-365` ; `MissionProcessV2.tsx:384` ; `src/index.css:368` | Variante « ai » neutre avec l'icône Sparkles, ou teinte `--k-accent-tint`. Pulse réservé à l'activité en cours. Attention : `Pill` est aussi utilisé par `ClientPortalV2`. |
| B-22 | P2 | Brief | La dictée aboutit à une impasse. Elle alimente `raw_brief`, qu'aucun champ n'affiche et que `computeCompletion` ne compte pas. « Analyser avec l'IA » reste désactivé sous 20 % : sur une mission créée « manuellement », un brief dicté en entier ne débloque pas l'analyse. | `v2/MissionBriefV2.tsx:233-236, 210, 1083-1107` | Verser la dictée dans « Mission » (`mission_description`), ou afficher un champ « Brief brut » éditable et compté. |
| B-23 | P2 | Brief et Sourcing | Deux calculs de complétude, tous deux affichés « /12 champs » avec des critères différents (langues, contexte, seuil de 30 caractères). | `MissionBriefV2.tsx:1083-1107` contre `src/lib/missionUtils.ts:8-22` (affiché dans `MissionSourcing.tsx:113` et `SourcingReadinessPanel.tsx:102`) | Une seule fonction exportée. |
| B-24 | P2 | Brief, Config | Sous `lg`, l'indicateur de sauvegarde automatique passe sous les sept sections du formulaire : invisible en mobile. « Erreur de sauvegarde » n'offre pas de reprise. Le bloc est copié à l'identique dans Config. | `MissionBriefV2.tsx:178, 259-288, 281-286` ; `MissionConfigV2.tsx:180-207` | Composant `SaveStatus` dans l'en-tête de vue, collant, avec « Réessayer ». |
| B-25 | P2 | Transverse | Des emoji servent d'icônes : titres de section du Brief, catégories de critères (🔧🤝🌟🎯💼), statuts en pastilles de couleur (🟢🟡🔵⚪), sections de la liste (⚡📍📦), sous-onglets Outreach (⚡📨), verdicts (✅❌🤔⏳), recommandations Insights, formats d'entretien (🎥📞🤝), « 👋 ». 72 lignes au total. | `MissionBriefV2.tsx:345, 533, 594, 638, 725, 868, 921, 947-951, 993, 1047` ; `MissionConfigV2.tsx:31-34, 94, 257, 379, 486` ; `MissionProcessV2.tsx:210, 217, 330, 391, 467` ; `MissionInsights.tsx:45, 130-197, 240, 283, 316` ; `process/shared.tsx:92-94, 389` ; `MissionOutreach.tsx:113-114` ; `ProjectsListV2.tsx:616, 638, 664` ; `pages/Qualification.tsx:39-42` ; `pages/MissionWorkspace.tsx:38` ; `pages/AcceptMissionInvite.tsx:54` ; `onboarding/WelcomeOnboardingModal.tsx:119` | Icônes lucide de 16 px en `text-muted-foreground`, ou aucune icône. |
| B-26 | P2 | Transverse | 252 tailles `text-[Npx]` (10, 10.5, 11, 11.5, 12, 12.5, 13, 14, 15, 20, 24, 28, 32 px) malgré l'échelle `2xs`/`3xs` créée pour les remplacer. | `tailwind.config.ts:143-149` ; exemples `MissionOverviewV2.tsx:236, 286, 297, 381, 421` ; `MissionWorkspaceV2.tsx:200` | Échelle fermée (11, 12, 14, 16, 20, 24, 30 px) et un codemod. |
| B-27 | P2 | Transverse | Jusqu'à quatre familles par écran. Le `h1` de /missions mêle Outfit et Instrument Serif italique. La création utilise le serif italique. L'onboarding associe serif italique et Space Mono (compteurs, fil des chapitres, score). `font-black uppercase` apparaît dans l'état vide et la revue des filtres. Le texte courant est en `font-medium` par défaut. | `ProjectsListV2.tsx:547-551` ; `CreateMissionV2.tsx:727-729` ; `onboarding/OnboardingShell.tsx:73, 91` ; `onboarding/SceneLaunch.tsx:55, 61` ; `VoiceDictation.tsx:202` ; `Qualification.tsx:253` ; `EmptyMissionState.tsx:335, 439, 521` ; `src/index.css:235` | Instrument Sans pour le texte, Outfit pour les titres (600). Serif réservé au site marketing. Chiffres en `tabular-nums`, sans mono. |
| B-28 | P2 | Transverse | Les primitives sont contournées : 112 boutons bruts contre 28 `Button`, 38 champs natifs. Le Brief redéfinit ses propres `TextInput`, `TextArea` et `Select` natif (menus système en thème sombre), tout comme Config et le Process. `Button` offre pourtant déjà `loading` et un anneau de focus. | `MissionBriefV2.tsx:397-459` ; `MissionConfigV2.tsx:305-349` ; `process/shared.tsx:486-505` ; `MissionOutreach.tsx:168-176` ; `src/components/ui/button.tsx:12-20, 40-66` | Utiliser `Button`, `Input`, `Textarea` et `Select` shadcn partout. |
| B-29 | P2 | Brief, Config, Process, création, Qualification, onboarding | Les libellés ne sont pas associés aux champs (`<label>` sans `htmlFor`, champs sans `id`) : le nom accessible retombe sur le placeholder (« Ex: Senior React Engineer ») ou manque (selects). L'interrupteur « Anonymiser le client » n'a pas de nom accessible. | `MissionBriefV2.tsx:390-395` ; `MissionConfigV2.tsx:288-302, 446-463` ; `process/shared.tsx:223-265, 283, 316, 361, 385` ; `MissionHuntMode.tsx:240-277` ; `MissionClientPortal.tsx:124-145` ; `CreateMissionV2.tsx:825-843, 955-963, 1063-1095` ; `Qualification.tsx:346` ; `SceneOrgDetails.tsx:90, 115, 170, 188` | Composant `Field` (`Label htmlFor`, `id` généré, aide liée par `aria-describedby`), et `Switch` shadcn. |
| B-30 | P2 | Transverse | Des boutons icône n'ont pas de nom accessible : fermeture de la dictée, retrait de langue, objectifs, retrait de membre, fermeture de formulaires, chips de la revue des filtres, cases et menus du tableau, retour de Qualification, variantes compactes d'ajout à une mission. | `MissionBriefV2.tsx:227, 838-843` ; `process/shared.tsx:390-395, 407-409, 513-518, 544-552, 608-613` ; `FilterReviewModal.tsx:181-186, 223-225, 233-244, 373-378, 452-457` ; `ProjectCandidatesTableEnhanced.tsx:412-418, 430-433, 453-456, 466-474, 530-536` ; `Qualification.tsx:155-157` ; `AddToProjectButton.tsx:189-197, 209-221` | Composant `IconButton` avec `aria-label` obligatoire et `Tooltip`. |
| B-31 | P2 | Liste, Pipeline | Les menus d'actions n'apparaissent qu'au survol (`opacity-0 group-hover:opacity-100`), sans `focus-visible:opacity-100` : invisibles au clavier et au tactile. | `ProjectsListV2.tsx:294` ; `ProjectCandidatesTableEnhanced.tsx:533` | Visibles à 60 % par défaut, à 100 % au survol ou au focus, toujours visibles sous `md`. |
| B-32 | P2 | Liste | La carte mission est une `div` avec `onClick`, sans `role` ni `tabIndex`, et le titre n'est pas un lien : on ne peut pas ouvrir une mission au clavier depuis la carte. | `ProjectsListV2.tsx:188-196, 215` | Titre en `Link` avec zone cliquable étendue (`after:absolute after:inset-0`), actions au-dessus. |
| B-33 | P2 | Liste | « Archiver » ou « Marquer terminée » ne donnent aucun retour : pas de toast de succès, et la carte part dans la section « Terminées · archivées », repliée par défaut. | `ProjectsListV2.tsx:503-507, 368, 656-676` ; `src/hooks/useSourcingProjects.ts:148-183` (aucun toast de succès) | Toast « Mission archivée » avec « Annuler ». |
| B-34 | P2 | Transverse | Terminologie mission ou projet : « Projet créé avec succès », « Projet supprimé », « Retirer du projet », « Aucun candidat dans ce projet », « +N autres projets », fichier CSV « candidats-projet ». | `useSourcingProjects.ts:140, 199` ; `ProjectCandidatesTableEnhanced.tsx:282, 318, 343, 607, 633` ; `AddToProjectButton.tsx:76, 247` | Le mot « mission » partout dans l'UI. |
| B-35 | P2 | Transverse | Anglicismes : « Sourcing & Outreach », « Outreach », « Insights », Hiring manager, Equity, Must-have, Should-have, Nice-to-have, red flags, Deal-breaker, Screening call, Culture fit, Lead, Sourcer, Account Manager, Reviewer, Table/Kanban, « Recherche Boolean », « wording », « parle people », « careers », Go/No Go, « Rejoindre le call ». Coquille : « Regénérer ». | `src/lib/missionViews.ts:42-55` ; `MissionBriefV2.tsx:676, 800, 870-906, 1047` ; `process/shared.tsx:34-66, 501-504, 694-698` ; `MissionPipeline.tsx:525, 537` ; `MissionConfigV2.tsx:380, 428-429` ; `CreateMissionV2.tsx:874` ; `Qualification.tsx:39-40, 231` ; `FilterReviewModal.tsx:135, 503` | Glossaire : garder « sourcing » et « pipeline » ; Outreach devient Approche, Insights Analyse, Must-have Indispensable. |
| B-36 | P2 | Transverse | Boutons en capitales espacées à côté de boutons en casse phrase (« LANCER LE SOURCING », « DICTER LE BRIEF », « ALLER AU BRIEF », « VOIR LA MISSION »…). | `FilterReviewModal.tsx:501, 510` ; `VoiceDictation.tsx:170, 190` ; `EmptyLinkedInAccountState.tsx:31` ; `MissionSourcing.tsx:119` ; `AcceptMissionInvite.tsx:46` ; `MissionInsights.tsx:300` ; `EmptyMissionState.tsx:550` | Casse phrase pour tous les boutons. Capitales réservées aux sur-titres de 11 px. |
| B-37 | P2 | Sourcing, Outreach, Insights, erreurs | Deux registres cohabitent dans le même espace : des boîtes brutalistes à angles vifs (`border border-border`, sans rayon) à côté des cartes `rounded-xl` des vues V2. | `MissionInsights.tsx:36, 234, 238, 281, 314` ; `MissionOutreach.tsx:119, 138` ; `MissionSourcing.tsx:90` ; `EmptyLinkedInAccountState.tsx:18-20` ; `src/components/SectionErrorBoundary.tsx:40-53` | Un seul conteneur `Card` (rayon, `bg-card`, filet discret). |
| B-38 | P2 | Sourcing | Une vidéo décorative en boucle (`/sourcing-empty-motion.mp4`, 1,1 Mo, lecture automatique, 288 à 384 px) occupe l'écran d'attente de chaque mission avant la première recherche. Le commentaire l'assume : « transformer le vide en moment de marque ». | `src/components/missions/SourcingReadinessPanel.tsx:63-76` ; affiché via `src/components/outreach/search/SearchResultsPanel.tsx:828` | Supprimer la vidéo. Garder un état vide texte avec les deux actions. |
| B-39 | P2 | Sourcing | Les pastilles « Brief x/12 » (état complet) et « crédits » (solde OK) sont en `text-accent` sur `bg-accent/5`. Or `--accent` vaut 40 3% 20 % en sombre (≈ 1,3:1) et 93 % en clair : l'état positif est le moins lisible. Le CTA « Lancer la recherche » est gris, moins visible que « Aller au brief ». | `SourcingReadinessPanel.tsx:90-121, 139-152` ; `src/index.css:32, 150` | Jetons `success` et `muted-foreground`. CTA en `Button primary`. |
| B-40 | P2 | Outreach | Les erreurs de chargement partent en console seulement : les stats restent à zéro sans message. « Créer une séquence » ne fait que sélectionner l'onglet Séquences, déjà ouvert par défaut. Les sous-onglets forment un troisième style d'onglets, avec emoji. Deux chargeurs se succèdent (BrutalLoader puis spinner). | `src/components/missions/MissionOutreach.tsx:96-105, 153-159, 28, 112-115, 201-216, 117-135` | `ErrorBox` avec « Réessayer ». Le CTA ouvre la création de séquence. `Tabs` partagés, un seul Skeleton. |
| B-41 | P2 | Pipeline, Insights | Couleurs arc-en-ciel codées en dur. Colonnes : cyan, `teal-400`, `indigo-400`, violet. Avatars : violet, bleu, cyan, rose, `emerald-400`, `amber-300`. Pastilles violettes pour les shortlistés. | `MissionPipeline.tsx:53-58, 84-91, 549` ; `MissionInsights.tsx:323` | Colonnes neutres (point gris), couleur réservée aux états sémantiques (embauché, écarté). Avatars neutres. |
| B-42 | P2 | Pipeline | Après un glisser-déposer, le toast de succès part avant la fin de la mise à jour (non attendue). Sans mise à jour optimiste, la carte revient dans sa colonne jusqu'au rechargement. L'erreur est générique. | `MissionPipeline.tsx:430-433, 336-357, 351` | Mutation React Query avec mise à jour optimiste (`onMutate`), retour arrière en cas d'échec, toast d'erreur avec « Réessayer ». |
| B-43 | P2 | Pipeline | Clavier : seul le `PointerSensor` est configuré, et Entrée sur une carte n'ouvre pas la fiche (`onClick` sur une `div`). La vue Tableau, affichée par défaut, n'ouvre pas la fiche au clic sur une ligne. | `MissionPipeline.tsx:332-334, 193-198, 284` ; `ProjectCandidatesTableEnhanced.tsx:448-451` | Ajouter `KeyboardSensor` et un menu « Déplacer vers… ». Ligne et carte ouvrent `CandidateDetailModal`. |
| B-44 | P2 | Pipeline | L'état vide global n'a pas d'action. Le tutoriel vidéo s'ouvre en modale 700 ms après la première visite. Les textes des colonnes vides sont désinvoltes (« Ça se joue à gauche », « Rien à écarter, bon signe »). | `MissionPipeline.tsx:568-577, 540, 109-115` ; `src/components/help/TutorialVideoDialog.tsx:60-70` | Bouton « Aller au sourcing ». Aide « ? » non intrusive. Ton neutre. |
| B-45 | P2 | Insights | Faux état vide : « Pas encore de données » s'affiche pendant le chargement, faute d'état de chargement. Le taux de réponse s'affiche à 0 % en rouge avant l'arrivée des données. « Taux de contact » apparaît deux fois sur l'onglet avec deux formules (contactés/total et contactés/évalués), et « Conversion globale » aussi deux fois. | `src/components/missions/MissionInsights.tsx:55-56, 62-100, 102, 112-114, 257, 271-276` ; `src/components/outreach/projects/ProjectFunnel.tsx:21, 141, 150-156` | Skeleton. Une seule rangée de KPI, une définition par métrique (infobulle de définition). |
| B-46 | P2 | Insights | ProjectFunnel sort du thème. La tuile `from-violet-50 to-violet-100/50`, texte `violet-600/700`, est claire en thème sombre. Les tuiles en dégradé ont un texte blanc (`*-foreground`) sur teinte claire en thème clair. `${stage.color}20` est du CSS invalide (fond absent). | `ProjectFunnel.tsx:143-148, 136-156, 87-90` | Réécrire avec un `StatTile` neutre. Entonnoir en barres horizontales monochromes. |
| B-47 | P2 | Insights | Recommandations alarmistes : priorité haute sur fond rouge `destructive` (« Mission sans activité »), emoji, lien en capitales. | `MissionInsights.tsx:287-292, 130-203, 300` | Liste sobre, icône neutre, rouge réservé aux erreurs. |
| B-48 | P2 | Création de mission | Deux boutons « Fermer » se chevauchent : le X de l'en-tête maison et celui intégré à `DialogContent` (`absolute right-2 top-2`). | `v2/CreateMissionV2.tsx:608-615` ; `src/components/ui/dialog.tsx:45-48` | Retirer le X maison, ou masquer celui de `DialogContent`. |
| B-49 | P2 | Création de mission | Décor IA empilé : barre dégradée animée en haut, tuile dégradée avec reflet, badge « Brief en 60 secondes » en dégradé animé et texte dégradé, titre « l'IA fait le reste. » en serif italique, bandeau de détection violet-bleu, tuile dégradée et barres roses animées pendant l'analyse. | `CreateMissionV2.tsx:576, 591-593, 723-729, 751-767, 926-945, 983-994` | Modale sobre : titre, deux options en liste, un seul CTA `primary`. |
| B-50 | P2 | État vide, création | Les promesses d'import dépassent la réalité. « Importez depuis une URL ou un fichier » : l'URL ne pré-remplit que titre, société et lieu à partir de l'adresse, et seuls les fichiers TXT et MD sont acceptés. « Ou importez depuis une page carrières » affiche une icône `cursor-pointer` sans action. | `EmptyMissionState.tsx:247, 569-571` ; `CreateMissionV2.tsx:439-446, 511-529, 873-879` | Libellés exacts (« Pré-remplir depuis un lien d'offre », « .txt ou .md ») ou prise en charge de PDF/DOCX. Retirer le lien mort. |
| B-51 | P2 | Création de mission | Les filtres générés sont enregistrés sans revue, puis le Sourcing s'ouvre. Cela contredit le principe écrit ailleurs d'une validation unique via la revue des filtres. | `CreateMissionV2.tsx:363-369, 430` ; `MissionSourcing.tsx:30-34, 104-108` | Ouvrir le Brief sur la revue des filtres, ou intégrer la revue dans la modale. |
| B-52 | P2 | Process | « Suggestion IA basée sur le brief », « Réoptimiser avec l'IA » et « Analyse du brief… » habillent un choix de template par mots-clés de séniorité (commentaire « rule-based »). | `v2/MissionProcessV2.tsx:98-123, 202, 486-488` | Libellé factuel : « Proposer un process selon la séniorité ». |
| B-53 | P2 | Process | Enregistrement au blur sans aucun indicateur, erreurs affichées en `err.message` brut. Réordonnancement par glisser HTML5 seulement, sans alternative clavier. | `process/shared.tsx:180-187, 224-229, 236-241, 158-167` ; `useMissionProcess.ts:172, 187, 202` | `SaveStatus` partagé. Boutons « Monter » et « Descendre », ou dnd-kit avec `KeyboardSensor`. |
| B-54 | P2 | Process | Le titre « Équipe mission » apparaît deux fois : titre de la carte, puis titre interne `text-[10px] uppercase` avec son propre séparateur. | `MissionProcessV2.tsx:329-333` ; `process/shared.tsx:465-471` | Retirer l'en-tête interne. |
| B-55 | P2 | Configuration | Trois modèles d'enregistrement sur une page : au blur (le « DebouncedInput » n'est pas debouncé), immédiat (select, boutons), et bouton explicite « Enregistrer les réglages » pour le mode chasse. L'indicateur ne couvre que le premier. | `MissionConfigV2.tsx:304-349, 120-122, 180-207` ; `MissionHuntMode.tsx:304-312` | Une seule règle : sauvegarde automatique partout avec un `SaveStatus` global, ou un formulaire explicite. |
| B-56 | P2 | Configuration | Chaque type d'organisation voit une carte morte : « Le mode chasse est réservé aux entreprises » ou « Le portail client est réservé aux cabinets et freelances », sans action. | `MissionConfigV2.tsx:172-173` ; `MissionHuntMode.tsx:86-95` ; `MissionClientPortal.tsx:41-50` | Masquer la section non applicable. |
| B-57 | P2 | Configuration | Statut de mission : `select` natif avec pastilles emoji, une pastille ajoutée devant le libellé qui contient déjà l'emoji (double indicateur), passage à « Archivé » sans confirmation. | `MissionConfigV2.tsx:30-35, 119-134, 214-220` | `Select` shadcn, `StatusBadge` partagé, confirmation pour l'archivage. |
| B-58 | P2 | Mode chasse | L'activation est une pilule dont le libellé est l'état (« Activé »/« Désactivé ») : on ne sait pas si l'on lit un état ou une action. | `MissionHuntMode.tsx:169-183` | `Switch` libellé « Mode chasse ». |
| B-59 | P2 | Portail client | Les accès sans `project_ids` (valables pour toutes les missions) sont listés dans chaque mission, et la révocation ne dit pas qu'elle les coupe partout. | `MissionClientPortal.tsx:37-39, 233-235` | Badge « Toutes les missions » et texte de confirmation adapté. |
| B-60 | P2 | Liste | Sur-titre « Tableau de bord » sur /missions. Cartes « à traiter » avec un dégradé de fond. Fond de la pastille de statut en CSS invalide (`${color}1a`). Pendant le chargement, les KPI et le sous-titre affichent 0. | `ProjectsListV2.tsx:544-546, 195, 216-219, 553-563, 584-589, 603-609` | `PageHeader` (« Missions », sous-titre factuel, bouton `primary`). Liseré ou point pour l'attention. Skeleton des KPI. |
| B-61 | P2 | Ajout à une mission | Seules les cinq premières missions sont proposées : « +N autres projets » est désactivé, et aucune recherche ne permet d'atteindre les suivantes. L'infobulle posée sur un bouton désactivé ne s'affiche pas. | `AddToProjectButton.tsx:228, 243-249, 186-203` | `Popover` avec `Command` et recherche. Infobulle sur un `span` qui enveloppe le bouton. |
| B-62 | P2 | Onboarding, société | Analyse simulée : les sources se cochent (« Base entreprises, LinkedIn, Web, WTTJ, Site carrière ») et les messages de l'agent défilent sur minuterie, indépendamment de l'appel réel. L'affichage est retardé d'au moins 2 s. Le dernier message utilise `konekt-shimmer-text`. | `onboarding/SceneOrganization.tsx:98-113, 139-148, 156-168, 406-415` | Une ligne d'état réelle (« Recherche de {nom}… ») et un Skeleton de la fiche, affichée dès réception. |
| B-63 | P2 | Onboarding | La variable CSS `--success` n'existe pas (seule `--status-success` est définie). L'arc du score de lancement a donc un trait invalide (anneau vide), la pastille « Enrichi » est sans couleur et un blob du fond disparaît. | `src/index.css:67` ; `onboarding/SceneLaunch.tsx:45` ; `SceneOrganization.tsx:513` ; `OnboardingBackdrop.tsx:33, 68` | `hsl(var(--status-success))` ou les classes `success`. |
| B-64 | P2 | Onboarding | Contenu hors sujet ou collecté pour rien : « Signal d'achat détecté » sur la société de l'utilisateur ; avatars `ui-avatars` en couleur aléatoire ; fourchette TJM demandée mais jamais enregistrée ; échec silencieux de la création des missions cochées. | `SceneOrganization.tsx:523-534, 487-503, 285-291` ; `SceneOrgDetails.tsx:106-163` ; `pages/Onboarding.tsx:214-219` (n'écrit pas `tjm`) | Retirer le signal et le TJM (ou enregistrer le TJM). Toast si les missions n'ont pas été créées. |
| B-65 | P2 | Onboarding | Registre décoratif : blobs animés de 46 à 58 s, grille, grain et vignette en fond. Interstitiel plein écran `backdrop-blur-xl` de 2,4 s entre chapitres. Transitions de 450 ms avec flou de 8 px. 210 confettis. Accent émeraude, qui n'est pas l'indigo cible. | `OnboardingBackdrop.tsx:22-100` ; `ChapterInterstitial.tsx:12, 36` ; `pages/Onboarding.tsx:298-310, 358` ; `ConfettiBurst.tsx:28, 72-74` ; `OnboardingShell.tsx:103` ; `EditorialChoiceList.tsx:104` ; `SceneOrganization.tsx:687` | Fond uni, pas d'interstitiel (le fil de chapitres suffit), fondu de 200 ms, accent `--k-accent`, célébration discrète (coche) au lieu des confettis. |
| B-66 | P2 | Onboarding | Textes : « Chapitre trois — votre linkedin. » (`toLowerCase()` sur « Votre LinkedIn »). « Réglages » alors que la navigation dit « Paramètres ». « Suivant » dans une scène, « Continuer » dans une autre. « Retour » tantôt `Button ghost`, tantôt lien texte. « prêt·e », « 👋 ». | `ChapterInterstitial.tsx:67` ; `SceneLaunch.tsx:135` ; `src/components/sidebar/SidebarBottomRow.tsx:50` ; `SceneOrgDetails.tsx:217, 228` ; `SceneSpecializations.tsx:76-82, 88` ; `WelcomeOnboardingModal.tsx:119, 189` | Relecture des chaînes : « Paramètres », « Continuer » partout, un seul style de retour. |
| B-67 | P2 | Qualification | États silencieux. La sauvegarde automatique toutes les 10 s n'a pas d'indicateur et ses échecs partent en console. La synchronisation du statut échoue en silence alors que l'écran promet « Ta décision mettra automatiquement à jour le statut ». Session introuvable : redirection vers /missions. Autres points : emoji, `amber-400` et `blue-400` codés en dur, « Scorecard sauvegardée » sur une page nommée « Qualification ». | `src/pages/Qualification.tsx:121-135, 104-116, 323, 65-68, 39-42, 201, 100` | `SaveStatus`, toast d'échec de synchronisation, `ToggleGroup` pour le verdict, état d'erreur en page. |
| B-68 | P2 | Coquille mission | « Mission introuvable » couvre aussi les erreurs réseau (`isError` n'est pas lu) et n'offre pas de reprise. On y trouve l'emoji 🔍 et un bouton brut. | `src/pages/MissionWorkspace.tsx:14, 31-54` | `ErrorState` (« Réessayer ») distinct de `EmptyState` (« Retour aux missions »). |
| B-69 | P2 | Transverse | Chargements hétérogènes : BrutalLoader (messages tournants « On prépare tout ça… » en capitales, shimmer), `Loader2`, trois spinners faits main et des Skeleton. | `src/components/ui/brutal-loader.tsx:4-18, 121-128, 141-148` ; utilisé par `MissionWorkspace.tsx:23`, `MissionSourcing.tsx:91`, `MissionOutreach.tsx:120`, `MissionPipeline.tsx:437` ; spinners `pages/Onboarding.tsx:328`, `pages/Qualification.tsx:140`, `process/shared.tsx:524` | Skeleton à la forme du contenu, un seul `Spinner` pour les attentes courtes. |
| B-70 | P2 | Transverse | Le mouvement ne respecte pas la préférence « mouvement réduit ». Pas de `MotionConfig` global. La media query ne couvre que `.interactive-card`, `.interactive-row` et `.stagger-in`. `konekt-fade-up` (480 ms) et les animations infinies `konekt-*` continuent. Seuls l'onboarding et AppLayout lisent `useReducedMotion`. | `src/index.css:424-427, 450-458` ; usages de `konekt-fade-up` dans `MissionOverviewV2.tsx:229, 282, 355`, `MissionProcessV2.tsx:286, 466`, `MissionPipeline.tsx:447, 548, 569, 595`, `CreateMissionV2.tsx:722, 736, 891, 927, 1022` | `<MotionConfig reducedMotion="user">` à la racine. Étendre la media query aux classes `konekt-*`. Durées plafonnées à 200 ms. |
| B-71 | P2 | Transverse | Des textes secondaires de 10 à 12 px en `text-muted-foreground/40` ou `/50` atteignent environ 2,3:1 sur fond sombre. | `EmptyMissionState.tsx:288, 569` ; `MissionPipeline.tsx:262, 272` ; `ProjectsListV2.tsx:346` ; `MissionOverviewV2.tsx:382` | `muted-foreground` plein pour tout texte. Opacité réservée aux icônes décoratives. |
| B-72 | P2 | Acceptation d'invitation | Titres de 14 px en capitales, bouton brut avec des classes résiduelles d'un effet retiré (`relative overflow-hidden group`, `span z-10`), emoji 🔒. Toutes les erreurs deviennent « n'est plus valide ou a expiré ». | `AcceptMissionInvite.tsx:42, 44-49, 54-58` | Carte centrée avec `EmptyState`/`ErrorState` partagés et un message par cause. |
| B-73 | P2 | Transverse | Messages d'erreur techniques affichés tels quels : « Erreur: {message Postgres} », `err.message` des mutations, de l'analyse IA, du scan d'URL et de l'IA vocale. | `useSourcingProjects.ts:143, 182, 202` ; `useMissionProcess.ts:142, 172, 187, 202, 228, 252` ; `MissionBriefV2.tsx:154` ; `CreateMissionV2.tsx:348, 498, 531` ; `VoiceDictation.tsx:136` | Messages humains par cas (« Impossible d'enregistrer. Vérifiez votre connexion. », avec action), détail technique en journal. |
| B-74 | P2 | Brief, revue des filtres | Le CTA « Lancer le sourcing » ne lance rien : il enregistre les filtres et ouvre l'onglet Sourcing (toast « Filtres sauvegardés — direction sourcing »). | `FilterReviewModal.tsx:516-517` ; `MissionBriefV2.tsx:160-172` | « Valider les filtres », puis la recherche se lance dans le Sourcing. |
| B-75 | P2 | Transverse | Le statut de mission est rendu de trois façons : point et libellé (`STATUS_DOT`), pastille à fond invalide (`STATUS_CONFIG`), emoji dans un select. | `MissionWorkspaceV2.tsx:84-89, 171-174` ; `ProjectsListV2.tsx:54-61, 216-222` ; `MissionConfigV2.tsx:30-35` | Composant `StatusBadge` unique. |
| B-76 | P3 | Brief, dictée | Rouges codés en dur (`red-600`, `red-500`), capitales, vouvoiement (« parlez naturellement ») dans un écran tutoyé. Deux clics nécessaires (« Dicter » puis « Dicter le brief »). | `VoiceDictation.tsx:170, 182, 190, 199-200, 216` ; `MissionBriefV2.tsx:193-206` | `Button variant="destructive"`, casse phrase, démarrage au premier clic. |
| B-77 | P3 | Vue d'ensemble | Logo client deviné par `nom.com` via `logo.clearbit.com` : risque d'afficher le logo d'une autre société. | `MissionOverviewV2.tsx:164-181` | N'afficher que `logo_url` ou le site renseigné. |
| B-78 | P3 | Liste | « Mise à jour il y a… » affiche en réalité la date de dernière recherche, ou à défaut la date de création. | `ProjectsListV2.tsx:184-185, 242` | Utiliser `updated_at`, ou libeller « Dernière recherche ». |
| B-79 | P3 | Coquille mission | Titres de repli d'erreur en franglais (« Erreur dans la Config », « l'Outreach », « les Insights »). Le repli affiche le message technique en police mono. | `MissionWorkspaceV2.tsx:258, 268, 278` ; `SectionErrorBoundary.tsx:42-49` | Titres en français, message technique masqué (déjà envoyé à Sentry). |
| B-80 | P3 | Revue des filtres | Les champs d'ajout de chips s'affichent en capitales (`uppercase`) alors que les chips restent en casse normale. | `FilterReviewModal.tsx:179, 221` | Retirer `uppercase`. |
| B-81 | P3 | Liste contre workspace | La liste est centrée (1200 px) alors que les vues mission sont alignées à gauche (960 et 1280 px, « plus de centrage »). | `ProjectsListV2.tsx:540` ; `MissionWorkspaceV2.tsx:217-228` | Même règle d'alignement pour toutes les pages de l'espace mission. |
| B-82 | P3 | Insights contre Pipeline | Le score s'écrit « 72% » dans l'activité récente, « 72 » dans le kanban et le tableau. | `MissionInsights.tsx:340` ; `MissionPipeline.tsx:151` ; `ProjectCandidatesTableEnhanced.tsx:499` | Un format unique (« 72 ») via `ScoreBadge`. |
| B-83 | P3 | Brief | Les compétences indispensables et les critères « Critique » sont codés en orange d'avertissement, un sens sémantique détourné. | `MissionBriefV2.tsx:469-473, 960-964, 1043` | Neutre plus graisse, ou teinte `--k-accent` pour la priorité. |
| B-84 | P3 | Liste | L'état « Aucune mission trouvée » invite à effacer la recherche sans bouton pour le faire. Le champ de recherche n'a qu'un placeholder, sans libellé. | `ProjectsListV2.tsx:594-599, 699-707` | Bouton « Effacer la recherche » et `aria-label`. |
| B-85 | P3 | Liste, Qualification | Bascule d'archive sans `aria-expanded`. Boutons de verdict sans `aria-pressed` ni groupe radio. | `ProjectsListV2.tsx:658-675` ; `Qualification.tsx:327-340` | `Collapsible` et `ToggleGroup` shadcn. |
| B-86 | P3 | Pipeline | Hauteurs « magiques » dépendantes de la mise en page. | `MissionPipeline.tsx:595` (`h-[calc(100dvh-340px)]`) ; `ProjectCandidatesTableEnhanced.tsx:425` (`h-[calc(100vh-400px)]`) | Hauteur en flex (`flex-1 min-h-0`) depuis la coquille corrigée en B-15. |
| B-87 | P3 | Accueil collaborateur | Tuile d'icône à angles vifs et ombre `hsl(var(--primary) / .15)`, soit une lueur blanche en thème sombre. | `onboarding/CollaboratorWelcome.tsx:18, 22, 62` | Supprimer l'ombre, rayon standard. |
| B-88 | P3 | Onboarding, société | L'onglet actif est souligné en `border-border`, de la même couleur que la piste : on ne distingue l'actif que par la couleur du texte. Onglets en capitales, sans `role="tab"`. | `SceneOrganization.tsx:547-561` | `Tabs` shadcn. |
| B-89 | P3 | Vue d'ensemble | La pastille « Outreach actif », avec pulse, dépend seulement de `stats_messaged > 0`, même si toutes les séquences sont terminées. | `MissionOverviewV2.tsx:360-366` | Dériver de l'état réel des inscriptions, ou afficher un libellé non temporel (« Candidats contactés »). |
| B-90 | P3 | Code mort | Trois composants orphelins. `CreateProjectModal` reproduit l'ancien flux de création (scan simulé, emoji, capitales) et afficherait `role.source` et `sourcesUsed` (« Apollo ») s'il était rebranché. | `src/components/missions/FilterWizard.tsx` ; `src/components/missions/PedigreePresetSelector.tsx` ; `src/components/outreach/projects/CreateProjectModal.tsx` (exporté par `index.ts:1`, jamais importé) | Supprimer, après une recherche d'import (déjà négative). |

---

## 2. Par écran : les 3 à 5 changements qui feraient passer au niveau « calm premium »

### 1. Liste des missions (/missions : `Outreach.tsx` et `ProjectsListV2`)
1. `PageHeader` partagé : titre « Missions » en Outfit 600, sous-titre factuel (« 4 actives · 2 à traiter »), bouton `primary` plein « Nouvelle mission » (B-10, B-60).
2. KPI exacts et nommés comme dans le pipeline (Sourcés, Contactés, Shortlistés), avec un Skeleton pendant le chargement (B-05, B-60).
3. Carte = titre en lien focalisable, menu d'actions toujours accessible, « à traiter » signalé par un point ou un liseré sans dégradé, `StatusBadge` unique (B-31, B-32, B-60, B-75).
4. Retour d'action : toast « Mission archivée » avec « Annuler » (B-33).
5. Vouvoiement et une seule famille de titres (B-13, B-27).

### 2. État vide de /missions (`EmptyMissionState`)
1. Repartir de la PR #245 puis retirer aussi la StatsBar non sourcée, les logos d'intégrations inexistantes et le lien « page carrières » mort (B-09, B-50).
2. Deux cartes `rounded-xl` qui sont chacune un seul bouton, avec anneau de focus visible, sans bouton imbriqué (B-09).
3. Titre en casse phrase (Outfit 600, 24-30 px), une phrase d'explication, puces neutres (B-27, B-36).
4. Aucun mouvement continu : un fondu d'entrée de 200 ms au maximum (B-70).

### 3. Création de mission (`CreateMissionV2`)
1. Supprimer barre, badges, tuiles et textes en dégradé ainsi que le serif italique ; un seul CTA `primary` par étape (B-49, B-10).
2. Un seul bouton Fermer (B-48).
3. Libellés d'import exacts (lien d'offre = pré-remplissage ; fichiers .txt et .md) (B-50).
4. Montrer la revue des filtres avant d'enregistrer, ou ouvrir le Brief au lieu du Sourcing (B-51).
5. Libellés associés aux champs, vouvoiement (B-29, B-13).

### 4. Coquille mission (`MissionWorkspace`, `MissionWorkspaceV2`, `PhaseStepper`)
1. Hauteur corrigée (`h-full min-h-0`) : un seul défilement (B-15).
2. Stepper honnête : état issu de la complétude, verrou expliqué au survol et au focus, transitions de 150 ms, compact en mobile (B-04, B-16, B-17, B-18).
3. Sous-onglets en `Tabs` accessibles, icône `Lock` au lieu de l'emoji (B-16, B-18).
4. En-tête de vue commun à toutes les vues, un seul `h1` (B-19).
5. Chargement en Skeleton ; « introuvable » distinct de « erreur » avec « Réessayer » (B-68, B-69).

### 5. Vue d'ensemble (`MissionOverviewV2`)
1. Process réel lu dans `mission_process_steps`, état vide avec action sinon (B-02).
2. Funnel branché sur `useProjectStats`, mêmes colonnes que le pipeline (B-03).
3. Une ligne « Prochaine étape » sobre, fondée sur `computeNextStep` (B-20).
4. Pastilles neutres sans dégradé ni pulse, tailles de l'échelle (B-21, B-26).

### 6. Brief (`MissionBriefV2`, `FilterReviewModal`, `VoiceDictation`)
1. Réécrire la revue des filtres sur `Dialog` : overlay sombre, focus piégé, Échap, casse phrase, CTA « Valider les filtres » (B-08, B-74).
2. Composants `Field`, `Input` et `Select` shadcn, `SectionCard` partagé sans emoji (B-28, B-29, B-25).
3. `SaveStatus` collant dans l'en-tête avec « Réessayer » (B-24).
4. La dictée remplit un champ visible et compté (B-22, B-23).
5. Bouton « Analyser » plein, sans dégradé ni reflet ; libellés en français (B-10, B-35).

### 7. Process (`MissionProcessV2`, `process/shared.tsx`)
1. Suppression d'étape confirmée avec le nombre de candidats concernés, ou suivie d'un « Annuler » (B-14).
2. Libellé honnête pour la proposition de template (B-52).
3. `SaveStatus` et alternative clavier au glisser (B-53).
4. Un seul titre « Équipe », rôles en français, boutons icône nommés (B-54, B-35, B-30).

### 8. Configuration (`MissionConfigV2`, `MissionHuntMode`, `MissionClientPortal`)
1. Un seul modèle d'enregistrement pour toute la page (B-55).
2. Masquer la section non disponible pour le type d'organisation (B-56).
3. `Select` et `StatusBadge` pour le statut, confirmation d'archivage (B-57).
4. `Switch` libellé pour le mode chasse et l'anonymisation (B-58, B-29).
5. Vouvoiement sur toute la page, comme le mode chasse (B-13).

### 9. Sourcing (enveloppe mission : `MissionSourcing`, `SourcingReadinessPanel`, `EmptyLinkedInAccountState`)
1. Retirer la vidéo en boucle ; état vide texte avec deux actions (« Aller au brief », « Lancer la recherche ») (B-38).
2. Pastilles lisibles avec des jetons sémantiques (B-39).
3. Carte arrondie et boutons en casse phrase, y compris pour l'état « Connectez LinkedIn » (B-37, B-36).
4. Un seul calcul de complétude (B-23).

### 10. Outreach (`MissionOutreach`)
1. Erreurs visibles avec « Réessayer » (B-40).
2. Le CTA « Créer une séquence » ouvre vraiment la création (B-40).
3. `Tabs` partagés sans emoji, un seul chargement Skeleton (B-40, B-69).
4. Libellés en français (« Approche » ou « Séquences ») (B-35).

### 11. Pipeline (`MissionPipeline`, `ProjectCandidatesTableEnhanced`)
1. `ScoreBadge` lisible dans les deux thèmes (B-11).
2. « Envoyer un message » branché, ligne et carte qui ouvrent la fiche, `KeyboardSensor` (B-12, B-43).
3. Colonnes et avatars neutres, couleur réservée à Embauché et Écarté (B-41).
4. Mise à jour optimiste avec retour arrière, toast après confirmation (B-42).
5. État vide avec action, aide non intrusive (B-44).

### 12. Insights (`MissionInsights`, `ProjectFunnel`)
1. Skeleton pendant le chargement, fini le faux « Pas encore de données » (B-45).
2. Une rangée de KPI, une définition par métrique, pas de doublons (B-45).
3. `ProjectFunnel` réécrit en barres monochromes, sans violet-50 ni dégradé (B-46).
4. Recommandations en liste neutre, rouge réservé aux erreurs, sans emoji (B-47).
5. Cartes arrondies comme les autres vues (B-37).

### 13. Onboarding (tunnel, `WelcomeOnboardingModal`, `CollaboratorWelcome`)
1. Retirer le nom « Apollo » des sources de postes (B-07).
2. Remplacer l'analyse simulée par un état réel et un Skeleton (B-62).
3. Fond uni, suppression de l'interstitiel et des confettis, fondus de 200 ms, accent indigo (B-65).
4. Corriger `--success` et les textes (« Paramètres », « LinkedIn », « Continuer ») (B-63, B-66).
5. Ne demander que ce qui est enregistré (TJM), retirer le « signal d'achat » (B-64).

### 14. Qualification (`pages/Qualification.tsx`)
1. `SaveStatus` visible, avec échec signalé (B-67).
2. Échec de synchronisation du statut signalé à l'utilisateur (B-67).
3. Verdict en `ToggleGroup` sans emoji, avec des couleurs de jetons (B-67, B-85).
4. Vouvoiement, bouton retour nommé (B-13, B-30).

### 15. Acceptation d'invitation (`pages/AcceptMissionInvite.tsx`)
1. Un seul appel d'acceptation (B-01).
2. Messages distincts par cause, « Réessayer » pour le réseau (B-01, B-72).
3. Carte sobre, sans emoji ni capitales, avec `Button` (B-72).

---

## 3. Ce qui fonctionne déjà bien et doit servir de modèle

- **Confirmations précises** : le mode chasse confirme chaque action lourde avec ses conséquences exactes (`MissionHuntMode.tsx:36-57, 456-509`). Le remplacement d'un process indique le nombre de candidats repositionnés (`MissionProcessV2.tsx:150-178`). La suppression d'une mission, la révocation d'un accès client et le retrait d'un membre passent par une `AlertDialog` en français (`ProjectsListV2.tsx:722-746`, `MissionClientPortal.tsx:230-257`, `process/shared.tsx:666-687`).
- **Erreur avec reprise** : `ErrorBox` (titre humain, détail secondaire, « Réessayer » en `Button`, `src/components/marketplace/ErrorBox.tsx`) utilisé dans `MissionHuntMode.tsx:360-365`. C'est la base de l'`ErrorState` partagé.
- **États vides qui mènent à l'action suivante** : mode chasse (« Publiez la mission pour recevoir des candidatures », `MissionHuntMode.tsx:366-371`), colonne « Contacté » vide avec « Contacter les candidats » (`MissionPipeline.tsx:260-270`), invitation à configurer le process depuis le pipeline (`MissionPipeline.tsx:547-565`).
- **Sauvegarde automatique du Brief** : quatre états explicites, reprise de la saisie tapée pendant une requête, envoi forcé au démontage (`MissionBriefV2.tsx:57-126, 259-288`). À extraire en `SaveStatus`.
- **Brouillon conservé** à la fermeture de la création, avec le toast « Brouillon repris » et sa date (`CreateMissionV2.tsx:259-306`). Placeholder multi-ligne concret (`CreateMissionV2.tsx:961`), exemples dans le Brief (« Ex: Senior React Engineer », « ASAP, T3 2026… », `MissionBriefV2.tsx:537, 563`), Skeleton pendant l'analyse (`CreateMissionV2.tsx:1009-1019`).
- **Encart de plafond** : gris neutre, texte adapté au rôle, cible de 44 px en mobile (`min-h-11 md:min-h-0`, `MissionQuotaNotice.tsx:33`).
- **Pipeline** : bascule Tableau/Kanban en `role="group"` avec `aria-pressed` (`MissionPipeline.tsx:514-539`) ; lien LinkedIn nommé et révélé au focus (`:166-176`) ; signaux de stagnation et taux de passage utiles (`:382-394, 449-501`).
- **Mouvement réduit respecté** dans l'onboarding (`pages/Onboarding.tsx:74, 291-296`, `OnboardingBackdrop.tsx`, `ChapterInterstitial.tsx:21-26`, `ConfettiBurst.tsx`) et dans AppLayout (transition de route de 220 ms). À généraliser.
- **Choix accessibles** : `EditorialChoiceList` en `radiogroup`/`radio` avec `aria-checked` et raccourcis clavier (`EditorialChoiceList.tsx:61, 74`). La scène LinkedIn relance la vérification au retour sur la fenêtre et propose « Connecter plus tard » (`SceneLinkedIn.tsx:61-89, 226-230`).
- **Coquille** : chaque vue est isolée par un `SectionErrorBoundary` avec « Réessayer ». Les liens directs `?tab=` sont préservés (`src/lib/missionViews.ts:74-77`). La transition d'onglet de 150 ms est dans la cible (`MissionWorkspaceV2.tsx:232-241`).
- **Primitives disponibles** : `Button` gère `loading`, l'anneau de focus et `rounded-full` (`src/components/ui/button.tsx`). Qualification et les scènes de l'onboarding (`SceneOrgDetails`, `SceneLinkedIn`, `SceneOrganization`) les utilisent déjà (`Card`, `Button`, `Select`, `Input`).

---

## 4. Motifs transverses et composants partagés manquants

**Motifs observés**
1. **Données affichées différentes des données réelles** : process inventé et funnel à zéro (vue d'ensemble), KPI « Réponses » qui compte des shortlistés, « Shortlister » qui n'en est pas un, étapes cochées selon leur position, analyse d'entreprise simulée, « IA » qui applique une règle.
2. **Trois registres visuels** : cartes V2 arrondies, blocs brutalistes (angles vifs, `font-black uppercase`) et éditorial (serif italique, mono). Aucun usage de l'accent indigo `--k-*`. Les accents concurrents sont le dégradé violet-rose animé, l'émeraude, `brand-purple`, l'orange d'avertissement et l'arc-en-ciel des colonnes.
3. **Typographie hors système** : 252 tailles arbitraires, 104 capitales espacées, jusqu'à quatre familles.
4. **Primitives contournées** : 112 boutons bruts, 38 champs natifs, libellés non associés, boutons icône sans nom, actions visibles au seul survol.
5. **États implicites** : faux états vides pendant les chargements, erreurs avalées (Outreach, Qualification, création des missions pendant l'onboarding), messages techniques bruts, trois modèles d'enregistrement, toast de succès avant confirmation, verrous muets.
6. **Mouvement décoratif continu** sur les actions principales, sans respect du mouvement réduit hors onboarding.
7. **Texte** : tutoiement et vouvoiement mélangés, mission et projet, anglicismes, casse des boutons, emoji en guise d'icônes.

**Composants partagés manquants** (nombre de variantes actuelles)
- `PageHeader` / `ViewHeader` : en-têtes faits à la main dans 5 écrans, absents dans 4 vues.
- `SectionCard` : 3 copies identiques (`MissionBriefV2.tsx:370`, `MissionProcessV2.tsx:410`, `MissionConfigV2.tsx:270`).
- `Field` (libellé, champ, aide, erreur) : `FieldLabel`, `Field` et au moins 6 libellés en ligne, aucun associé.
- `StatTile` / `KpiTile` : 8 variantes (vue d'ensemble, `KpiCard`, `KpiInline`, `KpiRow`, `MetricCard`, `ProjectFunnel`, funnel du pipeline, StatsBar).
- `StatusBadge` et `Pill` neutre : 6 traitements de pastilles, statut de mission rendu de 3 façons.
- `ScoreBadge` : 3 rendus du score (kanban, tableau, insights).
- `EmptyState` et `ErrorState` : une dizaine d'états vides en 5 traitements (emoji, carré lucide, rond lucide, texte seul, vidéo). `ErrorBox` à généraliser.
- `SaveStatus` : 2 copies, absent du Process et de Qualification.
- `Tabs` / `SegmentedControl` : 4 styles d'onglets dans le seul parcours mission.
- `IconButton` : `aria-label` obligatoire et `Tooltip`.
- `ChipInput` : `TagsInput` (Brief), `ChipInput` et `EditableChip` (revue des filtres), objectifs (Process), plus la copie morte de FilterWizard.
- `Avatar` neutre : initiales colorées dans le pipeline, icônes `User` ailleurs.
- `Skeleton` de page et `Spinner` unique, à la place de BrutalLoader et des trois spinners faits main.
