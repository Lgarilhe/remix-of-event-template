# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.*.local

node_modules
dist
dist-ssr
*.local

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tool state (not checked in)
.claude/settings.local.json
.claude/worktrees/
supabase/.temp/

# Playwright e2e auth states (sessions — ne jamais committer)
/e2e/.auth/
/e2e/specs/

# Harnais de vérification jetables des sessions Claude (app réelle + Supabase
# intercepté) — recréés à la demande, jamais committés
/.verify-*.mjs

# Lockfile généré par les vérifications deno locales (les fonctions sont bundlées par la CLI Supabase)
deno.lock
