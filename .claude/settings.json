{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit:*)",
            "command": "npx tsc --noEmit 2>&1 | tail -20; TSC_EXIT=${PIPESTATUS[0]}; if [ $TSC_EXIT -ne 0 ]; then echo '{\"decision\":\"block\",\"reason\":\"TypeScript errors found. Fix them before committing.\"}'; else echo '{\"decision\":\"allow\"}'; fi",
            "timeout": 120,
            "statusMessage": "Type-checking with tsc..."
          }
        ]
      }
    ]
  }
}
